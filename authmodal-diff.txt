diff --git a/frontend/src/components/auth/AuthModal.jsx b/frontend/src/components/auth/AuthModal.jsx
index 9fd3bc2..86cad6e 100644
--- a/frontend/src/components/auth/AuthModal.jsx
+++ b/frontend/src/components/auth/AuthModal.jsx
@@ -6,6 +6,7 @@ import { Link, useNavigate } from 'react-router-dom';
 import { getAllCities, getNeighborhoodsByCity } from '../../data/israelLocations.js';
 import SuccessModal from '../SuccessModal';
 import imageCompression from 'browser-image-compression';
+import ServiceDetailsForm from '../services/shared/ServiceDetailsForm';
 
 // Fonction de scroll automatique vers le premier champ en erreur
 const scrollToFirstError = (errors, currentStep = 1) => {
@@ -1286,4732 +1287,6 @@ case 'glass_works':
     return null;
   };
 
- const renderServiceDetailsForm = () => {
-  const { serviceDetails } = formData;
-
-  switch (formData.serviceType) {
-    case 'babysitting':
-      return (
-        <div className="service-details-form">
-          <h3>פרטי שירות שמרטפות</h3>
-          
-          <div className="form-section">
-            <h4>שדות חובה</h4>
-            
-            <div className="input-group">
-              <label>גיל</label>
-              <input
-                type="number"
-                value={serviceDetails.age || ''}
-                onChange={(e) => {
-                  const age = e.target.value;
-                  handleServiceDetailsChange('age', age);
-                  
-                  if (age && parseInt(age) < 13) {
-                    setErrors(prev => ({
-                      ...prev,
-                      'serviceDetails.age': 'גיל מינימלי: 13 שנים'
-                    }));
-                  } else {
-                    setErrors(prev => {
-                      const { 'serviceDetails.age': removed, ...rest } = prev;
-                      return rest;
-                    });
-                  }
-                }}
-                className={`standard-input ${errors['serviceDetails.age'] ? 'error' : ''}`}
-                data-field="age"
-                min="15"
-              />
-              {errors['serviceDetails.age'] && <span className="error-text">{errors['serviceDetails.age']}</span>}
-            </div>
-
-            <div className="input-group">
-              <label>שנות ניסיון עם ילדים</label>
-              <input
-                type="number"
-                value={serviceDetails.experience || ''}
-                onChange={(e) => handleServiceDetailsChange('experience', e.target.value)}
-                className={`standard-input ${errors['serviceDetails.experience'] ? 'error' : ''}`}
-                data-field="experience"
-              />
-              {errors['serviceDetails.experience'] && <span className="error-text">{errors['serviceDetails.experience']}</span>}
-            </div>
-
-            <div className="input-group">
-              <label>קבוצות גיל מתאימות</label>
-              <div className="checkbox-group" data-field="ageGroups">
-                {['0-1 שנה', '1-3 שנים', '3-6 שנים', '6+ שנים'].map(age => (
-                  <label key={age} className="checkbox-item">
-                    <input
-                      type="checkbox"
-                      checked={serviceDetails.ageGroups?.includes(age) || false}
-                      onChange={(e) => {
-                        const current = serviceDetails.ageGroups || [];
-                        const newAges = e.target.checked 
-                          ? [...current, age]
-                          : current.filter(a => a !== age);
-                        handleServiceDetailsChange('ageGroups', newAges);
-                      }}
-                    />
-                    {age}
-                  </label>
-                ))}
-              </div>
-              {errors['serviceDetails.ageGroups'] && <span className="error-text">{errors['serviceDetails.ageGroups']}</span>}
-            </div>
-
-            <div className="input-group">
-              <label>ימי זמינות</label>
-              <div className="checkbox-group" data-field="availability_days">
-                {['ראשון', 'שני', 'שלישי', 'רביעי', 'חמישי', 'שישי', 'כל השבוע'].map(day => (
-                  <label key={day} className="checkbox-item">
-                    <input
-                      type="checkbox"
-                      checked={serviceDetails.availability_days?.includes(day) || false}
-                      onChange={() => handleExclusiveCheckbox('availability_days', day, 'כל השבוע', ['ראשון', 'שני', 'שלישי', 'רביעי', 'חמישי', 'שישי'])}
-                    />
-                    {day}
-                  </label>
-                ))}
-              </div>
-              {errors['serviceDetails.availability_days'] && <span className="error-text">{errors['serviceDetails.availability_days']}</span>}
-            </div>
-
-            <div className="input-group">
-              <label>שעות זמינות</label>
-              <div className="checkbox-group" data-field="availability_hours">
-                {['בוקר', 'אחר הצהריים', 'ערב', 'הכל'].map(hour => (
-                  <label key={hour} className="checkbox-item">
-                    <input
-                      type="checkbox"
-                      checked={serviceDetails.availability_hours?.includes(hour) || false}
-                      onChange={() => handleExclusiveCheckbox('availability_hours', hour, 'הכל', ['בוקר', 'אחר הצהריים', 'ערב'])}
-                    />
-                    {hour}
-                  </label>
-                ))}
-              </div>
-              {errors['serviceDetails.availability_hours'] && <span className="error-text">{errors['serviceDetails.availability_hours']}</span>}
-            </div>
-
-            <div className="input-group">
-              <label>סוגי שמרטפות</label>
-              <div className="checkbox-group" data-field="babysitting_types">
-                {[
-                  'שמרטפות מזדמנת',
-                  'שמרטפות קבועה בבית הלקוח',
-                  'הוצאה מהגן / מבית-הספר',
-                  'שמירה בלילה',
-                  'שמירה בזמן חופשות',
-                  'עזרה בשיעורי בית',
-                  'מטפלת במשרה מלאה',
-                  'קייטנת קיץ',
-                  'קייטנת חורף'
-                ].map(type => (
-                  <label key={type} className="checkbox-item">
-                    <input
-                      type="checkbox"
-                      checked={serviceDetails.babysitting_types?.includes(type) || false}
-                      onChange={(e) => {
-                        const current = serviceDetails.babysitting_types || [];
-                        const newTypes = e.target.checked 
-                          ? [...current, type]
-                          : current.filter(t => t !== type);
-                        handleServiceDetailsChange('babysitting_types', newTypes);
-                      }}
-                    />
-                    {type}
-                  </label>
-                ))}
-              </div>
-              {errors['serviceDetails.babysitting_types'] && <span className="error-text">{errors['serviceDetails.babysitting_types']}</span>}
-            </div>
-
-            <div className="input-group">
-              <label>יכול להגיע ולחזור לבד</label>
-              <select
-                value={serviceDetails.can_travel_alone === true ? 'yes' : serviceDetails.can_travel_alone === false ? 'no' : ''}
-                onChange={(e) => handleServiceDetailsChange('can_travel_alone', e.target.value === 'yes')}
-                className={`standard-input ${errors['serviceDetails.can_travel_alone'] ? 'error' : ''}`}
-                data-field="can_travel_alone"
-              >
-                <option value="">בחר</option>
-                <option value="yes">כן</option>
-                <option value="no">לא</option>
-              </select>
-              {errors['serviceDetails.can_travel_alone'] && <span className="error-text">{errors['serviceDetails.can_travel_alone']}</span>}
-            </div>
-
-            <div className="input-group">
-              <label>שפות</label>
-              <div className="checkbox-group" data-field="languages">
-                {['עברית', 'ערבית', 'רוסית', 'אנגלית', 'ספרדית', 'צרפתית'].map(lang => (
-                  <label key={lang} className="checkbox-item">
-                    <input
-                      type="checkbox"
-                      checked={serviceDetails.languages?.includes(lang) || false}
-                      onChange={(e) => {
-                        const current = serviceDetails.languages || [];
-                        const newLangs = e.target.checked 
-                          ? [...current, lang]
-                          : current.filter(l => l !== lang);
-                        handleServiceDetailsChange('languages', newLangs);
-                      }}
-                    />
-                    {lang}
-                  </label>
-                ))}
-              </div>
-              {errors['serviceDetails.languages'] && <span className="error-text">{errors['serviceDetails.languages']}</span>}
-            </div>
-
-            <div className="input-group">
-              <label>תעריף שעתי (₪)</label>
-              <input
-                type="number"
-                value={serviceDetails.hourlyRate || ''}
-                onChange={(e) => handleServiceDetailsChange('hourlyRate', e.target.value)}
-                placeholder="לדוגמה: 50"
-                className={`standard-input ${errors['serviceDetails.hourlyRate'] ? 'error' : ''}`}
-                data-field="hourlyRate"
-              />
-              {errors['serviceDetails.hourlyRate'] && <span className="error-text">{errors['serviceDetails.hourlyRate']}</span>}
-            </div>
-          </div>
-
-          <div className="form-section optional">
-            <h4>שדות אופציונליים</h4>
-            
-            <div className="input-group">
-              <label>הסמכות</label>
-              <select
-                value={serviceDetails.certifications || ''}
-                onChange={(e) => handleServiceDetailsChange('certifications', e.target.value)}
-                className="standard-input"
-              >
-                <option value="">בחר הסמכה</option>
-                <option value="הכשרה בתחום החינוך המיוחד">הכשרה בתחום החינוך המיוחד</option>
-                <option value="קורס עזרה ראשונה">קורס עזרה ראשונה</option>
-                <option value="ניסיון בגני ילדים או מעונות">ניסיון בגני ילדים או מעונות</option>
-              </select>
-            </div>
-
-            <div className="input-group">
-              <label>רמת דתיות</label>
-              <select
-                value={serviceDetails.religiosity || ''}
-                onChange={(e) => handleServiceDetailsChange('religiosity', e.target.value)}
-                className="standard-input"
-              >
-                <option value="">בחר רמה</option>
-                <option value="">לא משנה</option>
-                <option value="חילוני">חילוני</option>
-                <option value="מסורתי">מסורתי</option>
-                <option value="דתי">דתי</option>
-                <option value="חרדי">חרדי</option>
-              </select>
-            </div>
-          </div>
-        </div>
-      );
-
-    case 'cleaning':
-      return (
-        <div className="service-details-form">
-          <h3>פרטי שירות ניקיון</h3>
-          
-          <div className="form-section">
-            <h4>שדות חובה</h4>
-            
-            <div className="input-group">
-              <label>סטטוס משפטי</label>
-              <select 
-                value={serviceDetails.legalStatus || ''} 
-                onChange={(e) => handleServiceDetailsChange('legalStatus', e.target.value)}
-                className={`standard-input ${errors['serviceDetails.legalStatus'] ? 'error' : ''}`}
-                data-field="legalStatus"
-              >
-                <option value="">בחר סטטוס</option>
-                <option value="חברה">חברה</option>
-                <option value="עצמאי">עצמאי</option>
-              </select>
-              {errors['serviceDetails.legalStatus'] && <span className="error-text">{errors['serviceDetails.legalStatus']}</span>}
-            </div>
-
-            <div className="input-group">
-              <label>קטגוריות ניקיון</label>
-              
-              <div className="category-group">
-                <h5 className="category-title">ניקיון ביתי</h5>
-                <div className="checkbox-group">
-                  {['ניקיון שוטף', 'ניקיון פסח', 'ניקיון אחרי שיפוץ', 'ניקיון לדירות Airbnb'].map(type => (
-                    <label key={type} className="checkbox-item">
-                      <input
-                        type="checkbox"
-                        checked={serviceDetails.cleaningTypes?.includes(type) || false}
-                        onChange={(e) => {
-                          const current = serviceDetails.cleaningTypes || [];
-                          const newTypes = e.target.checked 
-                            ? [...current, type]
-                            : current.filter(t => t !== type);
-                          handleServiceDetailsChange('cleaningTypes', newTypes);
-                        }}
-                      />
-                      {type}
-                    </label>
-                  ))}
-                </div>
-              </div>
-
-              <div className="category-group">
-                <h5 className="category-title">ניקיון משרדים ומבנים</h5>
-                <div className="checkbox-group">
-                  {['משרדים', 'חנויות', 'בניינים', 'מוסדות חינוך', 'מפעלים'].map(type => (
-                    <label key={type} className="checkbox-item">
-                      <input
-                        type="checkbox"
-                        checked={serviceDetails.cleaningTypes?.includes(type) || false}
-                        onChange={(e) => {
-                          const current = serviceDetails.cleaningTypes || [];
-                          const newTypes = e.target.checked 
-                            ? [...current, type]
-                            : current.filter(t => t !== type);
-                          handleServiceDetailsChange('cleaningTypes', newTypes);
-                        }}
-                      />
-                      {type}
-                    </label>
-                  ))}
-                </div>
-              </div>
-
-              <div className="category-group">
-                <h5 className="category-title">ניקיון מיוחד</h5>
-                <div className="checkbox-group">
-                  {['ניקוי חלונות','ניקוי מזגן','ריסוס (נגד חרקים)','ניקיון גגות רעפים', 'ניקוי שטיחים וספות', 'ניקוי וילונות', 'ניקוי בלחץ מים (טרסות, חזיתות)', 'חיטוי וניקיון אחרי נזק (שריפה / הצפה)'].map(type => (
-                    <label key={type} className="checkbox-item">
-                      <input
-                        type="checkbox"
-                        checked={serviceDetails.cleaningTypes?.includes(type) || false}
-                        onChange={(e) => {
-                          const current = serviceDetails.cleaningTypes || [];
-                          const newTypes = e.target.checked 
-                            ? [...current, type]
-                            : current.filter(t => t !== type);
-                          handleServiceDetailsChange('cleaningTypes', newTypes);
-                        }}
-                      />
-                      {type}
-                    </label>
-                  ))}
-                </div>
-              </div>
-
-              <div className="category-group">
-                <h5 className="category-title">שירותים נוספים</h5>
-                <div className="checkbox-group">
-                  {['ניקוי רכב בבית הלקוח', 'ניקוי פאנלים סולאריים'].map(type => (
-                    <label key={type} className="checkbox-item">
-                      <input
-                        type="checkbox"
-                        checked={serviceDetails.cleaningTypes?.includes(type) || false}
-                        onChange={(e) => {
-                          const current = serviceDetails.cleaningTypes || [];
-                          const newTypes = e.target.checked 
-                            ? [...current, type]
-                            : current.filter(t => t !== type);
-                          handleServiceDetailsChange('cleaningTypes', newTypes);
-                        }}
-                      />
-                      {type}
-                    </label>
-                  ))}
-                </div>
-              </div>
-              
-              {errors['serviceDetails.cleaningTypes'] && <span className="error-text">{errors['serviceDetails.cleaningTypes']}</span>}
-            </div>
-
-            <div className="input-group">
-              <label>תדירות זמינה</label>
-              <div className="checkbox-group" data-field="frequency">
-                {['חד פעמי', 'שבועי', 'דו שבועי', 'חודשי'].map(freq => (
-                  <label key={freq} className="checkbox-item">
-                    <input
-                      type="checkbox"
-                      checked={serviceDetails.frequency === freq}
-                      onChange={() => handleServiceDetailsChange('frequency', freq)}
-                    />
-                    {freq}
-                  </label>
-                ))}
-              </div>
-              {errors['serviceDetails.frequency'] && <span className="error-text">{errors['serviceDetails.frequency']}</span>}
-            </div>
-
-            <div className="input-group">
-              <label>תעריף</label>
-              <input
-                type="text"
-                value={serviceDetails.rate || ''}
-                onChange={(e) => handleServiceDetailsChange('rate', e.target.value)}
-                placeholder="לשעה או קבוע"
-                className={`standard-input ${errors['serviceDetails.rate'] ? 'error' : ''}`}
-                data-field="rate"
-              />
-              {errors['serviceDetails.rate'] && <span className="error-text">{errors['serviceDetails.rate']}</span>}
-            </div>
-
-            <div className="input-group">
-              <label>אספקת ציוד</label>
-              <select 
-                value={serviceDetails.materialsProvided || ''} 
-                onChange={(e) => handleServiceDetailsChange('materialsProvided', e.target.value)}
-                className={`standard-input ${errors['serviceDetails.materialsProvided'] ? 'error' : ''}`}
-                data-field="materialsProvided"
-              >
-                <option value="">בחר אפשרות</option>
-                <option value="yes">מספק ציוד</option>
-                <option value="no">לא מספק ציוד</option>
-                <option value="partial">ציוד חלקי</option>
-              </select>
-              {errors['serviceDetails.materialsProvided'] && <span className="error-text">{errors['serviceDetails.materialsProvided']}</span>}
-            </div>
-
-            <div className="input-group">
-              <label>זמינות</label>
-              
-              <div className="availability-subsection">
-                <h5 className="subsection-title">ימים</h5>
-                <div className="checkbox-group">
-                  {['ראשון', 'שני', 'שלישי', 'רביעי', 'חמישי', 'שישי', 'כל השבוע'].map(day => (
-                    <label key={day} className="checkbox-item">
-                      <input
-                        type="checkbox"
-                        checked={serviceDetails.availableDays?.includes(day) || false}
-                        onChange={() => handleExclusiveCheckbox('availableDays', day, 'כל השבוע', ['ראשון', 'שני', 'שלישי', 'רביעי', 'חמישי', 'שישי'])}
-                      />
-                      {day}
-                    </label>
-                  ))}
-                </div>
-              </div>
-
-              <div className="availability-subsection">
-                <h5 className="subsection-title">שעות</h5>
-                <div className="checkbox-group">
-                  {['בוקר', 'אחר הצהריים', 'ערב', 'הכל'].map(time => (
-                    <label key={time} className="checkbox-item">
-                      <input
-                        type="checkbox"
-                        checked={serviceDetails.availableHours?.includes(time) || false}
-                        onChange={() => handleExclusiveCheckbox('availableHours', time, 'הכל', ['בוקר', 'אחר הצהריים', 'ערב'])}
-                      />
-                      {time}
-                    </label>
-                  ))}
-                </div>
-              </div>
-            </div>
-          </div>
-
-          <div className="form-section optional">
-            <h4>שדות אופציונליים</h4>
-            
-            <div className="input-group">
-              <label>שנות ניסיון</label>
-              <input
-                type="number"
-                value={serviceDetails.experienceYears || ''}
-                onChange={(e) => handleServiceDetailsChange('experienceYears', e.target.value)}
-                className="standard-input"
-              />
-            </div>
-          </div>
-        </div>
-      );
-
-    case 'gardening':
-      return (
-        <div className="service-details-form">
-          <h3>פרטי שירות גינון</h3>
-          
-          <div className="form-section">
-            <h4>שדות חובה</h4>
-            
-            <div className="input-group">
-              <label>סוגי שירותי גינון</label>
-              <div className="checkbox-group" data-field="services">
-                {['גיזום עצים ושיחים', 'עיצוב גינה', 'שתילת צמחים', 'השקיה', 'דישון', 'ניכוש עשבים', 'תחזוקה כללית'].map(service => (
-                  <label key={service} className="checkbox-item">
-                    <input
-                      type="checkbox"
-                      checked={serviceDetails.services?.includes(service) || false}
-                      onChange={(e) => {
-                        const current = serviceDetails.services || [];
-                        const newServices = e.target.checked 
-                          ? [...current, service]
-                          : current.filter(s => s !== service);
-                        handleServiceDetailsChange('services', newServices);
-                      }}
-                    />
-                    {service}
-                  </label>
-                ))}
-              </div>
-              {errors['serviceDetails.services'] && <span className="error-text">{errors['serviceDetails.services']}</span>}
-            </div>
-
-            <div className="input-group">
-              <label>עונות זמינות</label>
-              <div className="checkbox-group" data-field="seasons">
-                {['כל השנה', 'אביב', 'קיץ', 'סתיו', 'חורף'].map(season => (
-                  <label key={season} className="checkbox-item">
-                    <input
-                      type="checkbox"
-                      checked={serviceDetails.seasons?.includes(season) || false}
-                      onChange={() => handleExclusiveCheckbox('seasons', season, 'כל השנה', ['אביב', 'קיץ', 'סתיו', 'חורף'])}
-                    />
-                    {season}
-                  </label>
-                ))}
-              </div>
-              {errors['serviceDetails.seasons'] && <span className="error-text">{errors['serviceDetails.seasons']}</span>}
-            </div>
-
-            <div className="input-group">
-              <label>ציוד בבעלותך</label>
-              <div className="checkbox-group" data-field="equipment">
-                {['מכסחת דשא', 'מזמרות גיזום', 'משאבת מים', 'כלים ידניים', 'מפזר דשן', 'מערכת השקיה'].map(equipment => (
-                  <label key={equipment} className="checkbox-item">
-                    <input
-                      type="checkbox"
-                      checked={serviceDetails.equipment?.includes(equipment) || false}
-                      onChange={(e) => {
-                        const current = serviceDetails.equipment || [];
-                        const newEquipment = e.target.checked 
-                          ? [...current, equipment]
-                          : current.filter(eq => eq !== equipment);
-                        handleServiceDetailsChange('equipment', newEquipment);
-                      }}
-                    />
-                    {equipment}
-                  </label>
-                ))}
-              </div>
-              {errors['serviceDetails.equipment'] && <span className="error-text">{errors['serviceDetails.equipment']}</span>}
-            </div>
-          </div>
-
-          <div className="form-section optional">
-            <h4>שדות אופציונליים</h4>
-            
-            <div className="input-group">
-              <label>התמחויות</label>
-              <div className="checkbox-group">
-                {['הכשרה גנן סוג א', 'הכשרה גנן סוג ב', 'אילני אגרונום', 'גוזם מומחה'].map(spec => (
-                  <label key={spec} className="checkbox-item">
-                    <input
-                      type="checkbox"
-                      checked={serviceDetails.specializations?.includes(spec) || false}
-                      onChange={(e) => {
-                        const current = serviceDetails.specializations || [];
-                        const newSpecs = e.target.checked 
-                          ? [...current, spec]
-                          : current.filter(s => s !== spec);
-                        handleServiceDetailsChange('specializations', newSpecs);
-                      }}
-                    />
-                    {spec}
-                  </label>
-                ))}
-              </div>
-            </div>
-
-            <div className="input-group">
-              <label> תעריף (₪)</label>
-              <input
-                type="text"
-                value={serviceDetails.rate || ''}
-                onChange={(e) => handleServiceDetailsChange('rate', e.target.value)}
-                placeholder="לשעה"
-                className={`standard-input ${errors['serviceDetails.rate'] ? 'error' : ''}`}
-                data-field="rate"
-              />
-              {errors['serviceDetails.rate'] && <span className="error-text">{errors['serviceDetails.rate']}</span>}
-            </div>
-
-            <div className="input-group">
-              <label>שירותים נוספים</label>
-              <div className="checkbox-group">
-                {['פינוי פסולת גינה', 'ייעוץ עיצוב נוף'].map(service => (
-                  <label key={service} className="checkbox-item">
-                    <input
-                      type="checkbox"
-                      checked={serviceDetails.additionalServices?.includes(service) || false}
-                      onChange={(e) => {
-                        const current = serviceDetails.additionalServices || [];
-                        const newServices = e.target.checked 
-                          ? [...current, service]
-                          : current.filter(s => s !== service);
-                        handleServiceDetailsChange('additionalServices', newServices);
-                      }}
-                    />
-                    {service}
-                  </label>
-                ))}
-              </div>
-            </div>
-          </div>
-        </div>
-      );
-
-    case 'petcare':
-      return (
-        <div className="service-details-form">
-          <h3>פרטי שירות שמירה על חיות מחמד</h3>
-          
-          <div className="form-section">
-            <h4>שדות חובה</h4>
-            
-            <div className="input-group">
-              <label>סוגי חיות מחמד</label>
-              <div className="checkbox-group" data-field="animalTypes">
-                {['כלבים', 'חתולים', 'ציפורים', 'מכרסמים קטנים', 'דגים', 'זוחלים'].map(animal => (
-                  <label key={animal} className="checkbox-item">
-                    <input
-                      type="checkbox"
-                      checked={serviceDetails.animalTypes?.includes(animal) || false}
-                      onChange={(e) => {
-                        const current = serviceDetails.animalTypes || [];
-                        const newTypes = e.target.checked 
-                          ? [...current, animal]
-                          : current.filter(a => a !== animal);
-                        handleServiceDetailsChange('animalTypes', newTypes);
-                      }}
-                    />
-                    {animal}
-                  </label>
-                ))}
-              </div>
-              {errors['serviceDetails.animalTypes'] && <span className="error-text">{errors['serviceDetails.animalTypes']}</span>}
-            </div>
-
-            {serviceDetails.animalTypes?.includes('כלבים') && (
-              <div className="input-group">
-                <label>גדלי כלבים</label>
-                <div className="checkbox-group" data-field="dogSizes">
-                  {[
-                    { size: 'קטן', weight: 'עד 10 ק״ג' },
-                    { size: 'בינוני', weight: '10–25 ק״ג' },
-                    { size: 'גדול', weight: '25–40 ק״ג' },
-                    { size: 'ענק', weight: 'מעל 40 ק״ג' }
-                  ].map(({ size, weight }) => (
-                    <label key={size} className="checkbox-item">
-                      <input
-                        type="checkbox"
-                        checked={serviceDetails.dogSizes?.includes(size) || false}
-                        onChange={(e) => {
-                          const current = serviceDetails.dogSizes || [];
-                          const newSizes = e.target.checked 
-                            ? [...current, size]
-                            : current.filter(s => s !== size);
-                          handleServiceDetailsChange('dogSizes', newSizes);
-                        }}
-                      />
-                      {size} / {weight}
-                    </label>
-                  ))}
-                </div>
-                {errors['serviceDetails.dogSizes'] && <span className="error-text">{errors['serviceDetails.dogSizes']}</span>}
-              </div>
-            )}
-
-            <div className="input-group">
-              <label>מקום השמירה</label>
-              <select
-                value={serviceDetails.location || ''}
-                onChange={(e) => handleServiceDetailsChange('location', e.target.value)}
-                className={`standard-input ${errors['serviceDetails.location'] ? 'error' : ''}`}
-                data-field="location"
-              >
-                <option value="">בחר</option>
-                <option value="בבית הלקוח">בבית הלקוח</option>
-                <option value="בבית המטפל">בבית המטפל</option>
-                <option value="פנסיון לבעלי חיים">פנסיון לבעלי חיים</option>
-              </select>
-              {errors['serviceDetails.location'] && <span className="error-text">{errors['serviceDetails.location']}</span>}
-            </div>
-
-            <div className="input-group">
-              <label>ניסיון עם חיות מחמד (שנים)</label>
-              <input
-                type="number"
-                value={serviceDetails.experience || ''}
-                onChange={(e) => handleServiceDetailsChange('experience', e.target.value)}
-                className={`standard-input ${errors['serviceDetails.experience'] ? 'error' : ''}`}
-                data-field="experience"
-              />
-              {errors['serviceDetails.experience'] && <span className="error-text">{errors['serviceDetails.experience']}</span>}
-            </div>
-          </div>
-
-          <div className="form-section optional">
-            <h4>שדות אופציונליים</h4>
-            
-            <div className="input-group">
-              <label>שירותים נוספים</label>
-              <div className="checkbox-group">
-                {[
-                  'הליכת כלבים',
-                  'רחצה וטיפוח',
-                  'אילוף בסיסי',
-                  'מתן תרופות',
-                  'האכלה בזמן השמירה',
-                  'ניקוי ארגז חול / כלוב / אקווריום',
-                  'עדכון תמונות לבעלים',
-                  'שהייה ביום בלבד',
-                  'לינה ללילה'
-                ].map(service => (
-                  <label key={service} className="checkbox-item">
-                    <input
-                      type="checkbox"
-                      checked={serviceDetails.additionalServices?.includes(service) || false}
-                      onChange={(e) => {
-                        const current = serviceDetails.additionalServices || [];
-                        const newServices = e.target.checked 
-                          ? [...current, service]
-                          : current.filter(s => s !== service);
-                        handleServiceDetailsChange('additionalServices', newServices);
-                      }}
-                    />
-                    {service}
-                  </label>
-                ))}
-              </div>
-            </div>
-
-            <div className="input-group">
-              <label>מתקנים בבית</label>
-              <div className="checkbox-group">
-                {['גינה מגודרת', 'חצר גדולה', 'מזגן'].map(facility => (
-                  <label key={facility} className="checkbox-item">
-                    <input
-                      type="checkbox"
-                      checked={serviceDetails.facilities?.includes(facility) || false}
-                      onChange={(e) => {
-                        const current = serviceDetails.facilities || [];
-                        const newFacilities = e.target.checked 
-                          ? [...current, facility]
-                          : current.filter(f => f !== facility);
-                        handleServiceDetailsChange('facilities', newFacilities);
-                      }}
-                    />
-                    {facility}
-                  </label>
-                ))}
-              </div>
-            </div>
-
-            <div className="input-group">
-              <label>שירותים וטרינריים</label>
-              <div className="checkbox-group">
-                {['ביקור וטרינר', 'טיפול בסיסי'].map(service => (
-                  <label key={service} className="checkbox-item">
-                    <input
-                      type="checkbox"
-                      checked={serviceDetails.veterinaryServices?.includes(service) || false}
-                      onChange={(e) => {
-                        const current = serviceDetails.veterinaryServices || [];
-                        const newServices = e.target.checked 
-                          ? [...current, service]
-                          : current.filter(s => s !== service);
-                        handleServiceDetailsChange('veterinaryServices', newServices);
-                      }}
-                    />
-                    {service}
-                  </label>
-                ))}
-              </div>
-            </div>
-          </div>
-        </div>
-      );
-
-    case 'tutoring':
-      return (
-        <div className="service-details-form">
-          <h3>פרטי שירות שיעורים פרטיים</h3>
-          
-          <div className="form-section">
-            <h4>שדות חובה</h4>
-            
-            <div className="input-group">
-              <label>מקצועות / נושאים</label>
-              {loadingSubcategories ? (
-                <div className="loading-subcategories">
-                  <Loader className="animate-spin" size={20} />
-                  <span>טוען מקצועות...</span>
-                </div>
-              ) : (
-                <div className="subcategories-container">
-                  {Object.entries(groupedTutoringSubcategories).map(([key, group]) => (
-                    <div key={key} className="subcategory-group">
-                      <h5 className="subcategory-title">{group.title}</h5>
-                      <div className="checkbox-group">
-                        {group.items.map(subject => (
-                          <label key={subject.id} className="checkbox-item">
-                            <input
-                              type="checkbox"
-                              checked={serviceDetails.subjects?.includes(subject.name_he) || false}
-                              onChange={(e) => {
-                                const current = serviceDetails.subjects || [];
-                                const newSubjects = e.target.checked 
-                                  ? [...current, subject.name_he]
-                                  : current.filter(s => s !== subject.name_he);
-                                handleServiceDetailsChange('subjects', newSubjects);
-                              }}
-                            />
-                            {subject.icon} {subject.name_he}
-                          </label>
-                        ))}
-                      </div>
-                    </div>
-                  ))}
-                </div>
-              )}
-              {errors['serviceDetails.subjects'] && <span className="error-text">{errors['serviceDetails.subjects']}</span>}
-            </div>
-
-            <div className="input-group">
-              <label>רמות לימוד</label>
-              <div className="checkbox-group" data-field="levels">
-                {['יסודי', 'חטיבת ביניים', 'תיכון', 'בגרות', 'מכינה', 'אקדמי', 'מבוגרים'].map(level => (
-                  <label key={level} className="checkbox-item">
-                    <input
-                      type="checkbox"
-                      checked={serviceDetails.levels?.includes(level) || false}
-                      onChange={(e) => {
-                        const current = serviceDetails.levels || [];
-                        const newLevels = e.target.checked 
-                          ? [...current, level]
-                          : current.filter(l => l !== level);
-                        handleServiceDetailsChange('levels', newLevels);
-                      }}
-                    />
-                    {level}
-                  </label>
-                ))}
-              </div>
-              {errors['serviceDetails.levels'] && <span className="error-text">{errors['serviceDetails.levels']}</span>}
-            </div>
-
-            <div className="input-group">
-              <label>השכלה והסמכות</label>
-              <input
-                type="text"
-                value={serviceDetails.qualifications || ''}
-                onChange={(e) => handleServiceDetailsChange('qualifications', e.target.value)}
-                placeholder="לדוגמה: תואר ראשון במתמטיקה"
-                className={`standard-input ${errors['serviceDetails.qualifications'] ? 'error' : ''}`}
-                data-field="qualifications"
-              />
-              {errors['serviceDetails.qualifications'] && <span className="error-text">{errors['serviceDetails.qualifications']}</span>}
-            </div>
-
-            <div className="input-group">
-              <label>אופן הוראה</label>
-              <select
-                value={serviceDetails.teachingMode || ''}
-                onChange={(e) => handleServiceDetailsChange('teachingMode', e.target.value)}
-                className={`standard-input ${errors['serviceDetails.teachingMode'] ? 'error' : ''}`}
-                data-field="teachingMode"
-              >
-                <option value="">בחר אופן</option>
-                <option value="פרונטלי בלבד">פרונטלי בלבד</option>
-                <option value="אונליין בלבד">אונליין בלבד</option>
-                <option value="שניהם">שניהם</option>
-              </select>
-              {errors['serviceDetails.teachingMode'] && <span className="error-text">{errors['serviceDetails.teachingMode']}</span>}
-            </div>
-          </div>
-
-          <div className="form-section optional">
-            <h4>שדות אופציונליים</h4>
-            
-            <div className="input-group">
-              <label>שנות ניסיון בהוראה</label>
-              <input
-                type="number"
-                value={serviceDetails.experienceYears || ''}
-                onChange={(e) => handleServiceDetailsChange('experienceYears', e.target.value)}
-                className="standard-input"
-              />
-            </div>
-
-            <div className="input-group">
-              <label>תעריף שעתי (₪)</label>
-              <input
-                type="number"
-                value={serviceDetails.hourlyRate || ''}
-                onChange={(e) => handleServiceDetailsChange('hourlyRate', e.target.value)}
-                className="standard-input"
-              />
-            </div>
-          </div>
-        </div>
-      );
-
-    case 'eldercare':
-      return (
-        <div className="service-details-form">
-          <h3>פרטי שירות טיפול בקשישים</h3>
-          
-          <div className="form-section">
-            <h4>שדות חובה</h4>
-            
-            <div className="input-group">
-              <label>סוגי טיפול</label>
-              <div className="checkbox-group" data-field="careTypes">
-                {['ליווי ותמיכה', 'עזרה בניקיון הבית', 'בישול והכנת אוכל', 'קניות ומשימות', 'מתן תרופות', 'ליווי לרופאים'].map(type => (
-                  <label key={type} className="checkbox-item">
-                    <input
-                      type="checkbox"
-                      checked={serviceDetails.careTypes?.includes(type) || false}
-                      onChange={(e) => {
-                        const current = serviceDetails.careTypes || [];
-                        const newTypes = e.target.checked 
-                          ? [...current, type]
-                          : current.filter(t => t !== type);
-                        handleServiceDetailsChange('careTypes', newTypes);
-                      }}
-                    />
-                    {type}
-                  </label>
-                ))}
-              </div>
-              {errors['serviceDetails.careTypes'] && <span className="error-text">{errors['serviceDetails.careTypes']}</span>}
-            </div>
-
-            <div className="input-group">
-              <label>הכשרה והסמכה</label>
-              <input
-                type="text"
-                value={serviceDetails.certification || ''}
-                onChange={(e) => handleServiceDetailsChange('certification', e.target.value)}
-                placeholder="לדוגמה: קורס סיעוד, עזרה ראשונה"
-                className={`standard-input ${errors['serviceDetails.certification'] ? 'error' : ''}`}
-                data-field="certification"
-              />
-              {errors['serviceDetails.certification'] && <span className="error-text">{errors['serviceDetails.certification']}</span>}
-            </div>
-
-            <div className="input-group">
-              <label>זמינות</label>
-              <div className="checkbox-group" data-field="availability">
-                {['בוקר', 'צהריים', 'אחר הצהריים', 'ערב', 'לילה', '24/7'].map(time => (
-                  <label key={time} className="checkbox-item">
-                    <input
-                      type="checkbox"
-                      checked={serviceDetails.availability?.includes(time) || false}
-                      onChange={(e) => {
-                        const current = serviceDetails.availability || [];
-                        const newAvailability = e.target.checked 
-                          ? [...current, time]
-                          : current.filter(t => t !== time);
-                        handleServiceDetailsChange('availability', newAvailability);
-                      }}
-                    />
-                    {time}
-                  </label>
-                ))}
-              </div>
-              {errors['serviceDetails.availability'] && <span className="error-text">{errors['serviceDetails.availability']}</span>}
-            </div>
-
-            <div className="input-group">
-              <label>ניסיון עם קשישים (שנים)</label>
-              <input
-                type="number"
-                value={serviceDetails.experience || ''}
-                onChange={(e) => handleServiceDetailsChange('experience', e.target.value)}
-                className={`standard-input ${errors['serviceDetails.experience'] ? 'error' : ''}`}
-                data-field="experience"
-              />
-              {errors['serviceDetails.experience'] && <span className="error-text">{errors['serviceDetails.experience']}</span>}
-            </div>
-          </div>
-
-          <div className="form-section optional">
-            <h4>שדות אופציונליים</h4>
-            
-            <div className="input-group">
-              <label>ניסיון עם מצבים מיוחדים</label>
-              <div className="checkbox-group">
-                {['אלצהיימר', 'פרקינסון', 'סוכרת', 'בעיות ניידות', 'דמנציה'].map(condition => (
-                  <label key={condition} className="checkbox-item">
-                    <input
-                      type="checkbox"
-                      checked={serviceDetails.specialConditions?.includes(condition) || false}
-                      onChange={(e) => {
-                        const current = serviceDetails.specialConditions || [];
-                        const newConditions = e.target.checked 
-                          ? [...current, condition]
-                          : current.filter(c => c !== condition);
-                        handleServiceDetailsChange('specialConditions', newConditions);
-                      }}
-                    />
-                    {condition}
-                  </label>
-                ))}
-              </div>
-            </div>
-
-            <div className="input-group">
-              <label>שפות</label>
-              <input
-                type="text"
-                value={serviceDetails.languages || ''}
-                onChange={(e) => handleServiceDetailsChange('languages', e.target.value)}
-                placeholder="עברית, אנגלית, רוסית וכו'"
-                className="standard-input"
-              />
-            </div>
-          </div>
-        </div>
-      );
-
-    case 'laundry':
-      return (
-        <div className="service-details-form">
-          <h3>פרטי שירות מכבסה וגיהוץ</h3>
-          
-          <div className="form-section">
-            <h4>שדות חובה</h4>
-            
-            <div className="input-group">
-              <label>סוגי שירותים</label>
-              <div className="checkbox-group" data-field="laundryTypes">
-                {[
-                  'גיהוץ בבית הלקוח',
-                  'איסוף והחזרת כביסה (שירות משלוחים)',
-                  'ניקוי יבש / שירות מכבסה',
-                  'כביסת מצעים, מגבות, וילונות',
-                  'כביסה תעשייתית (מלונות, מסעדות)'
-                ].map(type => (
-                  <label key={type} className="checkbox-item">
-                    <input
-                      type="checkbox"
-                      checked={serviceDetails.laundryTypes?.includes(type) || false}
-                      onChange={(e) => {
-                        const current = serviceDetails.laundryTypes || [];
-                        const newTypes = e.target.checked 
-                          ? [...current, type]
-                          : current.filter(t => t !== type);
-                        handleServiceDetailsChange('laundryTypes', newTypes);
-                      }}
-                    />
-                    {type}
-                  </label>
-                ))}
-              </div>
-              {errors['serviceDetails.laundryTypes'] && <span className="error-text">{errors['serviceDetails.laundryTypes']}</span>}
-            </div>
-
-            <div className="input-group">
-              <label>תעריף</label>
-              <input
-                type="text"
-                value={serviceDetails.rate || ''}
-                onChange={(e) => handleServiceDetailsChange('rate', e.target.value)}
-                placeholder="לשעה או קבוע"
-                className={`standard-input ${errors['serviceDetails.rate'] ? 'error' : ''}`}
-                data-field="rate"
-              />
-              {errors['serviceDetails.rate'] && <span className="error-text">{errors['serviceDetails.rate']}</span>}
-            </div>
-
-            <div className="input-group">
-              <label>זמינות - ימים</label>
-              <div className="checkbox-group">
-                {['ראשון', 'שני', 'שלישי', 'רביעי', 'חמישי', 'שישי', 'כל השבוע'].map(day => (
-                  <label key={day} className="checkbox-item">
-                    <input
-                      type="checkbox"
-                      checked={serviceDetails.availableDays?.includes(day) || false}
-                      onChange={() => handleExclusiveCheckbox('availableDays', day, 'כל השבוע', ['ראשון', 'שני', 'שלישי', 'רביעי', 'חמישי', 'שישי'])}
-                    />
-                    {day}
-                  </label>
-                ))}
-              </div>
-            </div>
-
-            <div className="input-group">
-              <label>זמינות - שעות</label>
-              <div className="checkbox-group">
-                {['בוקר', 'אחר הצהריים', 'ערב', 'הכל'].map(time => (
-                  <label key={time} className="checkbox-item">
-                    <input
-                      type="checkbox"
-                      checked={serviceDetails.availableHours?.includes(time) || false}
-                      onChange={() => handleExclusiveCheckbox('availableHours', time, 'הכל', ['בוקר', 'אחר הצהריים', 'ערב'])}
-                    />
-                    {time}
-                  </label>
-                ))}
-              </div>
-            </div>
-          </div>
-
-          <div className="form-section optional">
-            <h4>שדות אופציונליים</h4>
-            
-            <div className="input-group">
-              <label>שירות איסוף והחזרה</label>
-              <select 
-                value={serviceDetails.pickupService || ''} 
-                onChange={(e) => handleServiceDetailsChange('pickupService', e.target.value)}
-                className="standard-input"
-              >
-                <option value="">בחר אפשרות</option>
-                <option value="yes">מספק שירות איסוף</option>
-                <option value="no">לא מספק שירות איסוף</option>
-              </select>
-            </div>
-
-            <div className="input-group">
-              <label>שנות ניסיון</label>
-              <input
-                type="number"
-                value={serviceDetails.experienceYears || ''}
-                onChange={(e) => handleServiceDetailsChange('experienceYears', e.target.value)}
-                className="standard-input"
-              />
-            </div>
-          </div>
-        </div>
-      );
-
-    case 'property_management':
-      return (
-        <div className="service-details-form">
-          <h3>פרטי שירות - ניהול דירות</h3>
-          
-          <div className="form-section">
-            <h4>שדות חובה</h4>
-            
-            <div className="input-group">
-              <label>סוגי ניהול</label>
-              
-              <div style={{marginBottom: '16px'}}>
-                <div style={{fontWeight: '600', marginBottom: '8px', color: '#374151'}}>
-                  🏠 ניהול השכרה לשנה מלאה
-                </div>
-                <div className="checkbox-group" data-field="management_type">
-                  {[
-                    'חיפוש ובדיקת שוכרים מתאימים',
-                    'חתימה על חוזה וניהול ערבויות',
-                    'גביית שכ"ד והעברת תשלומים לבעל הדירה',
-                    'בדיקת מצב הנכס לפני ואחרי תקופת השכירות',
-                    'העברת חשבונות השירותים (מים, חשמל, גז) על שם השוכר החדש'
-                  ].map((type) => (
-                    <label key={type} className="checkbox-item">
-                      <input
-                        type="checkbox"
-                        checked={serviceDetails.management_type?.includes(type) || false}
-                        onChange={(e) => {
-                          const current = serviceDetails.management_type || [];
-                          const newTypes = e.target.checked
-                            ? [...current, type]
-                            : current.filter(t => t !== type);
-                          handleServiceDetailsChange('management_type', newTypes);
-                        }}
-                      />
-                      {type}
-                    </label>
-                  ))}
-                </div>
-              </div>
-
-              <div style={{marginBottom: '16px'}}>
-                <div style={{fontWeight: '600', marginBottom: '8px', color: '#374151'}}>
-                  🏖️ השכרה לטווח קצר (Airbnb / Booking)
-                </div>
-                <div className="checkbox-group">
-                  {[
-                    'פרסום וניהול מודעות באתרים',
-                    'ניהול הזמנות ותקשורת עם אורחים',
-                    'קבלת אורחים / מסירת מפתחות',
-                    'ניקיון בין השהיות',
-                    'בדיקה תקופתית של הנכס',
-                    'תיקונים כלליים (חשמל, אינסטלציה, מזגן וכו׳)'
-                  ].map((type) => (
-                    <label key={type} className="checkbox-item">
-                      <input
-                        type="checkbox"
-                        checked={serviceDetails.management_type?.includes(type) || false}
-                        onChange={(e) => {
-                          const current = serviceDetails.management_type || [];
-                          const newTypes = e.target.checked
-                            ? [...current, type]
-                            : current.filter(t => t !== type);
-                          handleServiceDetailsChange('management_type', newTypes);
-                        }}
-                      />
-                      {type}
-                    </label>
-                  ))}
-                </div>
-              </div>
-
-              {errors['serviceDetails.management_type'] && (
-                <span className="error-text">{errors['serviceDetails.management_type']}</span>
-              )}
-            </div>
-          </div>
-
-          <div className="form-section optional">
-            <h4>שדות אופציונליים</h4>
-            <div className="input-group">
-              <label>שנות ניסיון</label>
-              <input
-                type="number"
-                value={serviceDetails.experienceYears || ''}
-                onChange={(e) => handleServiceDetailsChange('experienceYears', e.target.value)}
-                className="standard-input"
-              />
-            </div>
-          </div>
-        </div>
-      );
-
-    case 'electrician':
-      return (
-        <div className="service-details-form">
-          <h3>פרטי שירות חשמלאים</h3>
-          
-          <div className="form-section">
-            <h4>שדות חובה</h4>
-            
-            <div className="input-group">
-              <label>גיל</label>
-              <input
-                type="number"
-                value={serviceDetails.age || ''}
-                onChange={(e) => handleServiceDetailsChange('age', e.target.value)}
-                className={`standard-input ${errors['serviceDetails.age'] ? 'error' : ''}`}
-                data-field="age"
-                min="18"
-              />
-              {errors['serviceDetails.age'] && <span className="error-text">{errors['serviceDetails.age']}</span>}
-            </div>
-
-            <div className="input-group">
-              <label>שנות ניסיון</label>
-              <input
-                type="number"
-                value={serviceDetails.experience || ''}
-                onChange={(e) => handleServiceDetailsChange('experience', e.target.value)}
-                className={`standard-input ${errors['serviceDetails.experience'] ? 'error' : ''}`}
-                data-field="experience"
-              />
-              {errors['serviceDetails.experience'] && <span className="error-text">{errors['serviceDetails.experience']}</span>}
-            </div>
-
-            <div className="input-group">
-              <label>ימי זמינות</label>
-              <div className="checkbox-group" data-field="availability_days">
-                {['ראשון', 'שני', 'שלישי', 'רביעי', 'חמישי', 'שישי', 'כל השבוע'].map(day => (
-                  <label key={day} className="checkbox-item">
-                    <input
-                      type="checkbox"
-                      checked={serviceDetails.availability_days?.includes(day) || false}
-                      onChange={() => handleExclusiveCheckbox('availability_days', day, 'כל השבוע', ['ראשון', 'שני', 'שלישי', 'רביעי', 'חמישי', 'שישי'])}
-                    />
-                    {day}
-                  </label>
-                ))}
-              </div>
-              {errors['serviceDetails.availability_days'] && <span className="error-text">{errors['serviceDetails.availability_days']}</span>}
-            </div>
-
-            <div className="input-group">
-              <label>שעות זמינות</label>
-              <div className="checkbox-group" data-field="availability_hours">
-                {['בוקר', 'אחר הצהריים', 'ערב', 'הכל'].map(hour => (
-                  <label key={hour} className="checkbox-item">
-                    <input
-                      type="checkbox"
-                      checked={serviceDetails.availability_hours?.includes(hour) || false}
-                      onChange={() => handleExclusiveCheckbox('availability_hours', hour, 'הכל', ['בוקר', 'אחר הצהריים', 'ערב'])}
-                    />
-                    {hour}
-                  </label>
-                ))}
-              </div>
-              {errors['serviceDetails.availability_hours'] && <span className="error-text">{errors['serviceDetails.availability_hours']}</span>}
-            </div>
-
-            <div className="input-group">
-              <label>סוגי עבודה</label>
-              
-              {/* תיקונים */}
-              <div style={{marginBottom: '20px'}}>
-                <label className="checkbox-item" style={{fontWeight: 'bold'}}>
-                  <input
-                    type="checkbox"
-                    checked={serviceDetails.work_types?.includes('תיקונים') || false}
-                    onChange={(e) => {
-                      const current = serviceDetails.work_types || [];
-                      const newTypes = e.target.checked 
-                        ? [...current, 'תיקונים']
-                        : current.filter(t => t !== 'תיקונים');
-                      handleServiceDetailsChange('work_types', newTypes);
-                    }}
-                  />
-                  תיקונים
-                </label>
-                
-                {serviceDetails.work_types?.includes('תיקונים') && (
-                  <div style={{marginRight: '30px', marginTop: '10px'}}>
-                    <div className="checkbox-group" data-field="repair_types">
-                      {['תיקון קצר', 'תיקון טיימר', 'תיקון לוח חשמל', 'החלפת שקעים', 'תיקון\\החלפת ספוטים', 'תיקונים אחרים', 'החלפת אוטומט חדר מדרגות'].map(type => (
-                        <label key={type} className="checkbox-item">
-                          <input
-                            type="checkbox"
-                            checked={serviceDetails.repair_types?.includes(type) || false}
-                            onChange={(e) => {
-                              const current = serviceDetails.repair_types || [];
-                              const newTypes = e.target.checked 
-                                ? [...current, type]
-                                : current.filter(t => t !== type);
-                              handleServiceDetailsChange('repair_types', newTypes);
-                            }}
-                          />
-                          {type}
-                        </label>
-                      ))}
-                    </div>
-                    {errors['serviceDetails.repair_types'] && <span className="error-text">{errors['serviceDetails.repair_types']}</span>}
-                  </div>
-                )}
-              </div>
-
-              {/* התקנות */}
-              <div style={{marginBottom: '20px'}}>
-                <label className="checkbox-item" style={{fontWeight: 'bold'}}>
-                  <input
-                    type="checkbox"
-                    checked={serviceDetails.work_types?.includes('התקנות') || false}
-                    onChange={(e) => {
-                      const current = serviceDetails.work_types || [];
-                      const newTypes = e.target.checked 
-                        ? [...current, 'התקנות']
-                        : current.filter(t => t !== 'התקנות');
-                      handleServiceDetailsChange('work_types', newTypes);
-                    }}
-                  />
-                  התקנות
-                </label>
-                
-                {serviceDetails.work_types?.includes('התקנות') && (
-                  <div style={{marginRight: '30px', marginTop: '10px'}}>
-                    <div className="checkbox-group" data-field="installation_types">
-                      {['התקנת מאוורר תקרה', 'התקנת שקע חשמל', 'התקנת נקודת חשמל חדשה', 'התקנת אטמור', 'התקנת מתג', 'עמדת טעינה לרכב חשמלי', 'התקנת שעון שבת', 'התקנות אחרות', 'עמדת טעינה לרכב חשמלי של חברת EV-Meter', 'התקנות כיריים אינדוקציה', 'התקנת תנור אמבטיה', 'התקנת גנרטור לבית פרטי', 'התקנת ונטה', 'עמדת טעינה לרכב חשמלי EV-EDGE'].map(type => (
-                        <label key={type} className="checkbox-item">
-                          <input
-                            type="checkbox"
-                            checked={serviceDetails.installation_types?.includes(type) || false}
-                            onChange={(e) => {
-                              const current = serviceDetails.installation_types || [];
-                              const newTypes = e.target.checked 
-                                ? [...current, type]
-                                : current.filter(t => t !== type);
-                              handleServiceDetailsChange('installation_types', newTypes);
-                            }}
-                          />
-                          {type}
-                        </label>
-                      ))}
-                    </div>
-                    {errors['serviceDetails.installation_types'] && <span className="error-text">{errors['serviceDetails.installation_types']}</span>}
-                  </div>
-                )}
-              </div>
-
-              {/* עבודות חשמל גדולות */}
-              <div style={{marginBottom: '20px'}}>
-                <label className="checkbox-item" style={{fontWeight: 'bold'}}>
-                  <input
-                    type="checkbox"
-                    checked={serviceDetails.work_types?.includes('עבודות חשמל גדולות') || false}
-                    onChange={(e) => {
-                      const current = serviceDetails.work_types || [];
-                      const newTypes = e.target.checked 
-                        ? [...current, 'עבודות חשמל גדולות']
-                        : current.filter(t => t !== 'עבודות חשמל גדולות');
-                      handleServiceDetailsChange('work_types', newTypes);
-                    }}
-                  />
-                  עבודות חשמל גדולות
-                </label>
-                
-                {serviceDetails.work_types?.includes('עבודות חשמל גדולות') && (
-                  <div style={{marginRight: '30px', marginTop: '10px'}}>
-                    <div className="checkbox-group" data-field="large_work_types">
-                      {['בניית תשתית חשמל בכל הבית', 'החלפת תשתית חשמל בכל הבית', 'החלפת לוח חשמל', 'הארקה', 'החלפה לתלת פאזי', 'הכנה לביקורת עבור חברת חשמל'].map(type => (
-                        <label key={type} className="checkbox-item">
-                          <input
-                            type="checkbox"
-                            checked={serviceDetails.large_work_types?.includes(type) || false}
-                            onChange={(e) => {
-                              const current = serviceDetails.large_work_types || [];
-                              const newTypes = e.target.checked 
-                                ? [...current, type]
-                                : current.filter(t => t !== type);
-                              handleServiceDetailsChange('large_work_types', newTypes);
-                            }}
-                          />
-                          {type}
-                        </label>
-                      ))}
-                    </div>
-                    {errors['serviceDetails.large_work_types'] && <span className="error-text">{errors['serviceDetails.large_work_types']}</span>}
-                  </div>
-                )}
-              </div>
-
-              {errors['serviceDetails.work_types'] && <span className="error-text">{errors['serviceDetails.work_types']}</span>}
-            </div>
-          </div>
-        </div>
-      );
-
-    case 'plumbing':
-      return (
-        <div className="service-details-form">
-          <h3>פרטי שירות אינסטלציה</h3>
-          
-          <div className="form-section">
-            <h4>שדות חובה</h4>
-            
-            <div className="input-group">
-              <label>גיל</label>
-              <input
-                type="number"
-                value={serviceDetails.age || ''}
-                onChange={(e) => handleServiceDetailsChange('age', e.target.value)}
-                className={`standard-input ${errors['serviceDetails.age'] ? 'error' : ''}`}
-                data-field="age"
-                min="18"
-              />
-              {errors['serviceDetails.age'] && <span className="error-text">{errors['serviceDetails.age']}</span>}
-            </div>
-
-            <div className="input-group">
-              <label>שנות ניסיון</label>
-              <input
-                type="number"
-                value={serviceDetails.experience || ''}
-                onChange={(e) => handleServiceDetailsChange('experience', e.target.value)}
-                className={`standard-input ${errors['serviceDetails.experience'] ? 'error' : ''}`}
-                data-field="experience"
-              />
-              {errors['serviceDetails.experience'] && <span className="error-text">{errors['serviceDetails.experience']}</span>}
-            </div>
-
-            <div className="input-group">
-              <label>ימי זמינות</label>
-              <div className="checkbox-group" data-field="availability_days">
-                {['ראשון', 'שני', 'שלישי', 'רביעי', 'חמישי', 'שישי', 'כל השבוע'].map(day => (
-                  <label key={day} className="checkbox-item">
-                    <input
-                      type="checkbox"
-                      checked={serviceDetails.availability_days?.includes(day) || false}
-                      onChange={() => handleExclusiveCheckbox('availability_days', day, 'כל השבוע', ['ראשון', 'שני', 'שלישי', 'רביעי', 'חמישי', 'שישי'])}
-                    />
-                    {day}
-                  </label>
-                ))}
-              </div>
-              {errors['serviceDetails.availability_days'] && <span className="error-text">{errors['serviceDetails.availability_days']}</span>}
-            </div>
-
-            <div className="input-group">
-              <label>שעות זמינות</label>
-              <div className="checkbox-group" data-field="availability_hours">
-                {['בוקר', 'אחר הצהריים', 'ערב', 'הכל'].map(hour => (
-                  <label key={hour} className="checkbox-item">
-                    <input
-                      type="checkbox"
-                      checked={serviceDetails.availability_hours?.includes(hour) || false}
-                      onChange={() => handleExclusiveCheckbox('availability_hours', hour, 'הכל', ['בוקר', 'אחר הצהריים', 'ערב'])}
-                    />
-                    {hour}
-                  </label>
-                ))}
-              </div>
-              {errors['serviceDetails.availability_hours'] && <span className="error-text">{errors['serviceDetails.availability_hours']}</span>}
-            </div>
-
-            <div className="input-group">
-              <label>סוגי עבודה</label>
-              
-              {/* סתימות */}
-              <div style={{marginBottom: '20px'}}>
-                <label className="checkbox-item" style={{fontWeight: 'bold'}}>
-                  <input
-                    type="checkbox"
-                    checked={serviceDetails.work_types?.includes('סתימות') || false}
-                    onChange={(e) => {
-                      const current = serviceDetails.work_types || [];
-                      const newTypes = e.target.checked 
-                        ? [...current, 'סתימות']
-                        : current.filter(t => t !== 'סתימות');
-                      handleServiceDetailsChange('work_types', newTypes);
-                    }}
-                  />
-                  סתימות
-                </label>
-                
-                {serviceDetails.work_types?.includes('סתימות') && (
-                  <div style={{marginRight: '30px', marginTop: '10px'}}>
-                    <div className="checkbox-group" data-field="blockage_types">
-                      {['פתיחת סתימה בבית', 'משאבה טבולה', 'פתיחת סתימה בבנין'].map(type => (
-                        <label key={type} className="checkbox-item">
-                          <input
-                            type="checkbox"
-                            checked={serviceDetails.blockage_types?.includes(type) || false}
-                            onChange={(e) => {
-                              const current = serviceDetails.blockage_types || [];
-                              const newTypes = e.target.checked 
-                                ? [...current, type]
-                                : current.filter(t => t !== type);
-                              handleServiceDetailsChange('blockage_types', newTypes);
-                            }}
-                          />
-                          {type}
-                        </label>
-                      ))}
-                    </div>
-                    {errors['serviceDetails.blockage_types'] && <span className="error-text">{errors['serviceDetails.blockage_types']}</span>}
-                  </div>
-                )}
-              </div>
-
-              {/* תיקון צנרת */}
-              <div style={{marginBottom: '20px'}}>
-                <label className="checkbox-item" style={{fontWeight: 'bold'}}>
-                  <input
-                    type="checkbox"
-                    checked={serviceDetails.work_types?.includes('תיקון צנרת') || false}
-                    onChange={(e) => {
-                      const current = serviceDetails.work_types || [];
-                      const newTypes = e.target.checked 
-                        ? [...current, 'תיקון צנרת']
-                        : current.filter(t => t !== 'תיקון צנרת');
-                      handleServiceDetailsChange('work_types', newTypes);
-                    }}
-                  />
-                  תיקון צנרת
-                </label>
-                
-                {serviceDetails.work_types?.includes('תיקון צנרת') && (
-                  <div style={{marginRight: '30px', marginTop: '10px'}}>
-                    <div className="checkbox-group" data-field="pipe_repair_types">
-                      {['תיקון צנרת גברית', 'תיקון נזקי צנרת בבית', 'תיקון נזקי צנרת בבניין', 'הגברת לחץ מים', 'תיקון צנרת בגינה', 'תיקוני צנרת אחרים', 'תיקון צנרת ביוב ללא הרס'].map(type => (
-                        <label key={type} className="checkbox-item">
-                          <input
-                            type="checkbox"
-                            checked={serviceDetails.pipe_repair_types?.includes(type) || false}
-                            onChange={(e) => {
-                              const current = serviceDetails.pipe_repair_types || [];
-                              const newTypes = e.target.checked 
-                                ? [...current, type]
-                                : current.filter(t => t !== type);
-                              handleServiceDetailsChange('pipe_repair_types', newTypes);
-                            }}
-                          />
-                          {type}
-                        </label>
-                      ))}
-                    </div>
-                    {errors['serviceDetails.pipe_repair_types'] && <span className="error-text">{errors['serviceDetails.pipe_repair_types']}</span>}
-                  </div>
-                )}
-              </div>
-
-              {/* עבודות גדולות */}
-              <div style={{marginBottom: '20px'}}>
-                <label className="checkbox-item" style={{fontWeight: 'bold'}}>
-                  <input
-                    type="checkbox"
-                    checked={serviceDetails.work_types?.includes('עבודות גדולות') || false}
-                    onChange={(e) => {
-                      const current = serviceDetails.work_types || [];
-                      const newTypes = e.target.checked 
-                        ? [...current, 'עבודות גדולות']
-                        : current.filter(t => t !== 'עבודות גדולות');
-                      handleServiceDetailsChange('work_types', newTypes);
-                    }}
-                  />
-                  עבודות גדולות
-                </label>
-                
-                {serviceDetails.work_types?.includes('עבודות גדולות') && (
-                  <div style={{marginRight: '30px', marginTop: '10px'}}>
-                    <div className="checkbox-group" data-field="large_work_types">
-                      {['החלפת צנרת בבית', 'החלפת צנרת בבניין', 'התקנת נקודות מים חדשות', 'החלפת קו ביוב בבית', 'החלפת קו ביוב בבניין', 'הקמת קו ביוב חדש', 'החלפת צנרת בגינה', 'התקנת מזח'].map(type => (
-                        <label key={type} className="checkbox-item">
-                          <input
-                            type="checkbox"
-                            checked={serviceDetails.large_work_types?.includes(type) || false}
-                            onChange={(e) => {
-                              const current = serviceDetails.large_work_types || [];
-                              const newTypes = e.target.checked 
-                                ? [...current, type]
-                                : current.filter(t => t !== type);
-                              handleServiceDetailsChange('large_work_types', newTypes);
-                            }}
-                          />
-                          {type}
-                        </label>
-                      ))}
-                    </div>
-                    {errors['serviceDetails.large_work_types'] && <span className="error-text">{errors['serviceDetails.large_work_types']}</span>}
-                  </div>
-                )}
-              </div>
-
-              {/* תיקון והתקנת אביזרי אינסטלציה */}
-              <div style={{marginBottom: '20px'}}>
-                <label className="checkbox-item" style={{fontWeight: 'bold'}}>
-                  <input
-                    type="checkbox"
-                    checked={serviceDetails.work_types?.includes('תיקון והתקנת אביזרי אינסטלציה') || false}
-                    onChange={(e) => {
-                      const current = serviceDetails.work_types || [];
-                      const newTypes = e.target.checked 
-                        ? [...current, 'תיקון והתקנת אביזרי אינסטלציה']
-                        : current.filter(t => t !== 'תיקון והתקנת אביזרי אינסטלציה');
-                      handleServiceDetailsChange('work_types', newTypes);
-                    }}
-                  />
-                  תיקון והתקנת אביזרי אינסטלציה
-                </label>
-                
-                {serviceDetails.work_types?.includes('תיקון והתקנת אביזרי אינסטלציה') && (
-                  <div style={{marginRight: '30px', marginTop: '10px'}}>
-                    <div className="checkbox-group" data-field="fixture_types">
-                      {['התקנת בר מים', 'ניאגרה סמויה', 'ברזים', 'ניאגרות ואסלות', 'מסנני מים', 'התקנת טוחן אשפה', 'תיקון טוחן אשפה', 'כיורים', 'הכנה למדיח כלים', 'אגנית למקלחון', 'אביזרים אחרים', 'סילוקית לאסלה', 'התקנת בידה', 'אסלה תלויה', 'אל חוזר לשעון מים', 'התקנת מערכות מים תת כיוריות'].map(type => (
-                        <label key={type} className="checkbox-item">
-                          <input
-                            type="checkbox"
-                            checked={serviceDetails.fixture_types?.includes(type) || false}
-                            onChange={(e) => {
-                              const current = serviceDetails.fixture_types || [];
-                              const newTypes = e.target.checked 
-                                ? [...current, type]
-                                : current.filter(t => t !== type);
-                              handleServiceDetailsChange('fixture_types', newTypes);
-                            }}
-                          />
-                          {type}
-                        </label>
-                      ))}
-                    </div>
-                    {errors['serviceDetails.fixture_types'] && <span className="error-text">{errors['serviceDetails.fixture_types']}</span>}
-                  </div>
-                )}
-              </div>
-
-              {errors['serviceDetails.work_types'] && <span className="error-text">{errors['serviceDetails.work_types']}</span>}
-            </div>
-          </div>
-        </div>
-      );
-
-    case 'air_conditioning':
-      return (
-        <div className="service-details-form">
-          <h3>פרטי שירות מיזוג אוויר</h3>
-          
-          <div className="form-section">
-            <h4>שדות חובה</h4>
-            
-            <div className="input-group">
-              <label>גיל</label>
-              <input
-                type="number"
-                value={serviceDetails.age || ''}
-                onChange={(e) => handleServiceDetailsChange('age', e.target.value)}
-                className={`standard-input ${errors['serviceDetails.age'] ? 'error' : ''}`}
-                data-field="age"
-                min="18"
-              />
-              {errors['serviceDetails.age'] && <span className="error-text">{errors['serviceDetails.age']}</span>}
-            </div>
-
-            <div className="input-group">
-              <label>שנות ניסיון</label>
-              <input
-                type="number"
-                value={serviceDetails.experience || ''}
-                onChange={(e) => handleServiceDetailsChange('experience', e.target.value)}
-                className={`standard-input ${errors['serviceDetails.experience'] ? 'error' : ''}`}
-                data-field="experience"
-              />
-              {errors['serviceDetails.experience'] && <span className="error-text">{errors['serviceDetails.experience']}</span>}
-            </div>
-
-            <div className="input-group">
-              <label>ימי זמינות</label>
-              <div className="checkbox-group" data-field="availability_days">
-                {['ראשון', 'שני', 'שלישי', 'רביעי', 'חמישי', 'שישי', 'כל השבוע'].map(day => (
-                  <label key={day} className="checkbox-item">
-                    <input
-                      type="checkbox"
-                      checked={serviceDetails.availability_days?.includes(day) || false}
-                      onChange={() => handleExclusiveCheckbox('availability_days', day, 'כל השבוע', ['ראשון', 'שני', 'שלישי', 'רביעי', 'חמישי', 'שישי'])}
-                    />
-                    {day}
-                  </label>
-                ))}
-              </div>
-              {errors['serviceDetails.availability_days'] && <span className="error-text">{errors['serviceDetails.availability_days']}</span>}
-            </div>
-
-            <div className="input-group">
-              <label>שעות זמינות</label>
-              <div className="checkbox-group" data-field="availability_hours">
-                {['בוקר', 'אחר הצהריים', 'ערב', 'הכל'].map(hour => (
-                  <label key={hour} className="checkbox-item">
-                    <input
-                      type="checkbox"
-                      checked={serviceDetails.availability_hours?.includes(hour) || false}
-                      onChange={() => handleExclusiveCheckbox('availability_hours', hour, 'הכל', ['בוקר', 'אחר הצהריים', 'ערב'])}
-                    />
-                    {hour}
-                  </label>
-                ))}
-              </div>
-              {errors['serviceDetails.availability_hours'] && <span className="error-text">{errors['serviceDetails.availability_hours']}</span>}
-            </div>
-
-            <div className="input-group">
-              <label>סוגי עבודה</label>
-              
-              {/* התקנת מזגנים */}
-              <div style={{marginBottom: '20px'}}>
-                <label className="checkbox-item" style={{fontWeight: 'bold'}}>
-                  <input
-                    type="checkbox"
-                    checked={serviceDetails.work_types?.includes('התקנת מזגנים') || false}
-                    onChange={(e) => {
-                      const current = serviceDetails.work_types || [];
-                      const newTypes = e.target.checked 
-                        ? [...current, 'התקנת מזגנים']
-                        : current.filter(t => t !== 'התקנת מזגנים');
-                      handleServiceDetailsChange('work_types', newTypes);
-                    }}
-                  />
-                  התקנת מזגנים
-                </label>
-                
-                {serviceDetails.work_types?.includes('התקנת מזגנים') && (
-                  <div style={{marginRight: '30px', marginTop: '10px'}}>
-                    <div className="checkbox-group" data-field="installation_types">
-                      {['התקנת מזגן', 'התקנת מיזוג מיני מרכזי', 'התקנת מיזוג מרכזי', 'התקנת מזגן אינוורטר', 'התקנת מזגן מולטי אינוורטר', 'התקנת מזגן VRF'].map(type => (
-                        <label key={type} className="checkbox-item">
-                          <input
-                            type="checkbox"
-                            checked={serviceDetails.installation_types?.includes(type) || false}
-                            onChange={(e) => {
-                              const current = serviceDetails.installation_types || [];
-                              const newTypes = e.target.checked 
-                                ? [...current, type]
-                                : current.filter(t => t !== type);
-                              handleServiceDetailsChange('installation_types', newTypes);
-                            }}
-                          />
-                          {type}
-                        </label>
-                      ))}
-                    </div>
-                    {errors['serviceDetails.installation_types'] && <span className="error-text">{errors['serviceDetails.installation_types']}</span>}
-                  </div>
-                )}
-              </div>
-
-              {/* תיקון מזגנים */}
-              <div style={{marginBottom: '20px'}}>
-                <label className="checkbox-item" style={{fontWeight: 'bold'}}>
-                  <input
-                    type="checkbox"
-                    checked={serviceDetails.work_types?.includes('תיקון מזגנים') || false}
-                    onChange={(e) => {
-                      const current = serviceDetails.work_types || [];
-                      const newTypes = e.target.checked 
-                        ? [...current, 'תיקון מזגנים']
-                        : current.filter(t => t !== 'תיקון מזגנים');
-                      handleServiceDetailsChange('work_types', newTypes);
-                    }}
-                  />
-                  תיקון מזגנים
-                </label>
-                
-                {serviceDetails.work_types?.includes('תיקון מזגנים') && (
-                  <div style={{marginRight: '30px', marginTop: '10px'}}>
-                    <div className="checkbox-group" data-field="repair_types">
-                      {['תיקון מזגן','מילוי גז', 'תיקון מזגן מעובש', 'תיקון מיזוג מיני מרכזי', 'תיקון דליפת גז במזגן', 'תיקון מיזוג מרכזי', 'תיקון מזגן אינוורטר', 'תיקון מזגן VRF', 'ניקוי פילטרים', 'תיקון צ\'ילרים', 'טכנאי חדרי קירור'].map(type => (
-                        <label key={type} className="checkbox-item">
-                          <input
-                            type="checkbox"
-                            checked={serviceDetails.repair_types?.includes(type) || false}
-                            onChange={(e) => {
-                              const current = serviceDetails.repair_types || [];
-                              const newTypes = e.target.checked 
-                                ? [...current, type]
-                                : current.filter(t => t !== type);
-                              handleServiceDetailsChange('repair_types', newTypes);
-                            }}
-                          />
-                          {type}
-                        </label>
-                      ))}
-                    </div>
-                    {errors['serviceDetails.repair_types'] && <span className="error-text">{errors['serviceDetails.repair_types']}</span>}
-                  </div>
-                )}
-              </div>
-
-              {/* פירוק והרכבת מזגנים */}
-              <div style={{marginBottom: '20px'}}>
-                <label className="checkbox-item" style={{fontWeight: 'bold'}}>
-                  <input
-                    type="checkbox"
-                    checked={serviceDetails.work_types?.includes('פירוק והרכבת מזגנים') || false}
-                    onChange={(e) => {
-                      const current = serviceDetails.work_types || [];
-                      const newTypes = e.target.checked 
-                        ? [...current, 'פירוק והרכבת מזגנים']
-                        : current.filter(t => t !== 'פירוק והרכבת מזגנים');
-                      handleServiceDetailsChange('work_types', newTypes);
-                    }}
-                  />
-                  פירוק והרכבת מזגנים
-                </label>
-                
-                {serviceDetails.work_types?.includes('פירוק והרכבת מזגנים') && (
-                  <div style={{marginRight: '30px', marginTop: '10px'}}>
-                    <div className="checkbox-group" data-field="disassembly_types">
-                      {['פירוק והרכבת מזגן', 'פירוק מיזוג מיני מרכזי', 'פירוק מיזוג מרכזי', 'פירוק מזגן אינוורטר', 'פירוק מזגן VRF'].map(type => (
-                        <label key={type} className="checkbox-item">
-                          <input
-                            type="checkbox"
-                            checked={serviceDetails.disassembly_types?.includes(type) || false}
-                            onChange={(e) => {
-                              const current = serviceDetails.disassembly_types || [];
-                              const newTypes = e.target.checked 
-                                ? [...current, type]
-                                : current.filter(t => t !== type);
-                              handleServiceDetailsChange('disassembly_types', newTypes);
-                            }}
-                          />
-                          {type}
-                        </label>
-                      ))}
-                    </div>
-                    {errors['serviceDetails.disassembly_types'] && <span className="error-text">{errors['serviceDetails.disassembly_types']}</span>}
-                  </div>
-                )}
-              </div>
-
-              {errors['serviceDetails.work_types'] && <span className="error-text">{errors['serviceDetails.work_types']}</span>}
-            </div>
-          </div>
-        </div>
-      );
-
-    case 'gas_technician':
-      return (
-        <div className="service-details-form">
-          <h3>פרטי שירות טכנאי גז</h3>
-          
-          <div className="form-section">
-            <h4>שדות חובה</h4>
-            
-            <div className="input-group">
-              <label>גיל</label>
-              <input
-                type="number"
-                value={serviceDetails.age || ''}
-                onChange={(e) => handleServiceDetailsChange('age', e.target.value)}
-                className={`standard-input ${errors['serviceDetails.age'] ? 'error' : ''}`}
-                data-field="age"
-                min="18"
-              />
-              {errors['serviceDetails.age'] && <span className="error-text">{errors['serviceDetails.age']}</span>}
-            </div>
-
-            <div className="input-group">
-              <label>שנות ניסיון</label>
-              <input
-                type="number"
-                value={serviceDetails.experience || ''}
-                onChange={(e) => handleServiceDetailsChange('experience', e.target.value)}
-                className={`standard-input ${errors['serviceDetails.experience'] ? 'error' : ''}`}
-                data-field="experience"
-              />
-              {errors['serviceDetails.experience'] && <span className="error-text">{errors['serviceDetails.experience']}</span>}
-            </div>
-
-            <div className="input-group">
-              <label>ימי זמינות</label>
-              <div className="checkbox-group" data-field="availability_days">
-                {['ראשון', 'שני', 'שלישי', 'רביעי', 'חמישי', 'שישי', 'כל השבוע'].map(day => (
-                  <label key={day} className="checkbox-item">
-                    <input
-                      type="checkbox"
-                      checked={serviceDetails.availability_days?.includes(day) || false}
-                      onChange={() => handleExclusiveCheckbox('availability_days', day, 'כל השבוע', ['ראשון', 'שני', 'שלישי', 'רביעי', 'חמישי', 'שישי'])}
-                    />
-                    {day}
-                  </label>
-                ))}
-              </div>
-              {errors['serviceDetails.availability_days'] && <span className="error-text">{errors['serviceDetails.availability_days']}</span>}
-            </div>
-
-            <div className="input-group">
-              <label>שעות זמינות</label>
-              <div className="checkbox-group" data-field="availability_hours">
-                {['בוקר', 'אחר הצהריים', 'ערב', 'הכל'].map(hour => (
-                  <label key={hour} className="checkbox-item">
-                    <input
-                      type="checkbox"
-                      checked={serviceDetails.availability_hours?.includes(hour) || false}
-                      onChange={() => handleExclusiveCheckbox('availability_hours', hour, 'הכל', ['בוקר', 'אחר הצהריים', 'ערב'])}
-                    />
-                    {hour}
-                  </label>
-                ))}
-              </div>
-              {errors['serviceDetails.availability_hours'] && <span className="error-text">{errors['serviceDetails.availability_hours']}</span>}
-            </div>
-
-            <div className="input-group">
-              <label>סוגי עבודה</label>
-              
-              {/* התקנת צנרת גז בבית */}
-              <div style={{marginBottom: '20px'}}>
-                <label className="checkbox-item" style={{fontWeight: 'bold'}}>
-                  <input
-                    type="checkbox"
-                    checked={serviceDetails.work_types?.includes('התקנת צנרת גז בבית') || false}
-                    onChange={(e) => {
-                      const current = serviceDetails.work_types || [];
-                      const newTypes = e.target.checked 
-                        ? [...current, 'התקנת צנרת גז בבית']
-                        : current.filter(t => t !== 'התקנת צנרת גז בבית');
-                      handleServiceDetailsChange('work_types', newTypes);
-                    }}
-                  />
-                  התקנת צנרת גז בבית
-                </label>
-                
-                {serviceDetails.work_types?.includes('התקנת צנרת גז בבית') && (
-                  <div style={{marginRight: '30px', marginTop: '10px'}}>
-                    <div className="checkbox-group" data-field="installation_types">
-                      {['הזזת\\התקנת נקודת גז', 'התקנת כיריים גז', 'התקנת צינור גז', 'התקנת גריל גז', 'התקנת חימום מים בגז', 'התקנת חגז', 'בניית תשתית גז במבנה חדש', 'שירותי גז לעסקים'].map(type => (
-                        <label key={type} className="checkbox-item">
-                          <input
-                            type="checkbox"
-                            checked={serviceDetails.installation_types?.includes(type) || false}
-                            onChange={(e) => {
-                              const current = serviceDetails.installation_types || [];
-                              const newTypes = e.target.checked 
-                                ? [...current, type]
-                                : current.filter(t => t !== type);
-                              handleServiceDetailsChange('installation_types', newTypes);
-                            }}
-                          />
-                          {type}
-                        </label>
-                      ))}
-                    </div>
-                    {errors['serviceDetails.installation_types'] && <span className="error-text">{errors['serviceDetails.installation_types']}</span>}
-                  </div>
-                )}
-              </div>
-
-              {/* תיקוני גז בבית */}
-              <div style={{marginBottom: '20px'}}>
-                <label className="checkbox-item" style={{fontWeight: 'bold'}}>
-                  <input
-                    type="checkbox"
-                    checked={serviceDetails.work_types?.includes('תיקוני גז בבית') || false}
-                    onChange={(e) => {
-                      const current = serviceDetails.work_types || [];
-                      const newTypes = e.target.checked 
-                        ? [...current, 'תיקוני גז בבית']
-                        : current.filter(t => t !== 'תיקוני גז בבית');
-                      handleServiceDetailsChange('work_types', newTypes);
-                    }}
-                  />
-                  תיקוני גז בבית
-                </label>
-                
-                {serviceDetails.work_types?.includes('תיקוני גז בבית') && (
-                  <div style={{marginRight: '30px', marginTop: '10px'}}>
-                    <div className="checkbox-group" data-field="repair_types">
-                      {['תיקון כיריים גז', 'תיקון צנרת גז'].map(type => (
-                        <label key={type} className="checkbox-item">
-                          <input
-                            type="checkbox"
-                            checked={serviceDetails.repair_types?.includes(type) || false}
-                            onChange={(e) => {
-                              const current = serviceDetails.repair_types || [];
-                              const newTypes = e.target.checked 
-                                ? [...current, type]
-                                : current.filter(t => t !== type);
-                              handleServiceDetailsChange('repair_types', newTypes);
-                            }}
-                          />
-                          {type}
-                        </label>
-                      ))}
-                    </div>
-                    {errors['serviceDetails.repair_types'] && <span className="error-text">{errors['serviceDetails.repair_types']}</span>}
-                  </div>
-                )}
-              </div>
-
-              {errors['serviceDetails.work_types'] && <span className="error-text">{errors['serviceDetails.work_types']}</span>}
-            </div>
-          </div>
-        </div>
-      );
-
-    case 'drywall':
-      return (
-        <div className="service-details-form">
-          <h3>פרטי שירות עבודות גבס</h3>
-          
-          <div className="form-section">
-            <h4>שדות חובה</h4>
-            
-            <div className="input-group">
-              <label>גיל</label>
-              <input
-                type="number"
-                value={serviceDetails.age || ''}
-                onChange={(e) => handleServiceDetailsChange('age', e.target.value)}
-                className={`standard-input ${errors['serviceDetails.age'] ? 'error' : ''}`}
-                data-field="age"
-                min="18"
-              />
-              {errors['serviceDetails.age'] && <span className="error-text">{errors['serviceDetails.age']}</span>}
-            </div>
-
-            <div className="input-group">
-              <label>שנות ניסיון</label>
-              <input
-                type="number"
-                value={serviceDetails.experience || ''}
-                onChange={(e) => handleServiceDetailsChange('experience', e.target.value)}
-                className={`standard-input ${errors['serviceDetails.experience'] ? 'error' : ''}`}
-                data-field="experience"
-              />
-              {errors['serviceDetails.experience'] && <span className="error-text">{errors['serviceDetails.experience']}</span>}
-            </div>
-
-            <div className="input-group">
-              <label>ימי זמינות</label>
-              <div className="checkbox-group" data-field="availability_days">
-                {['ראשון', 'שני', 'שלישי', 'רביעי', 'חמישי', 'שישי', 'כל השבוע'].map(day => (
-                  <label key={day} className="checkbox-item">
-                    <input
-                      type="checkbox"
-                      checked={serviceDetails.availability_days?.includes(day) || false}
-                      onChange={() => handleExclusiveCheckbox('availability_days', day, 'כל השבוע', ['ראשון', 'שני', 'שלישי', 'רביעי', 'חמישי', 'שישי'])}
-                    />
-                    {day}
-                  </label>
-                ))}
-              </div>
-              {errors['serviceDetails.availability_days'] && <span className="error-text">{errors['serviceDetails.availability_days']}</span>}
-            </div>
-
-            <div className="input-group">
-              <label>שעות זמינות</label>
-              <div className="checkbox-group" data-field="availability_hours">
-                {['בוקר', 'אחר הצהריים', 'ערב', 'הכל'].map(hour => (
-                  <label key={hour} className="checkbox-item">
-                    <input
-                      type="checkbox"
-                      checked={serviceDetails.availability_hours?.includes(hour) || false}
-                      onChange={() => handleExclusiveCheckbox('availability_hours', hour, 'הכל', ['בוקר', 'אחר הצהריים', 'ערב'])}
-                    />
-                    {hour}
-                  </label>
-                ))}
-              </div>
-              {errors['serviceDetails.availability_hours'] && <span className="error-text">{errors['serviceDetails.availability_hours']}</span>}
-            </div>
-
-            <div className="input-group">
-              <label>סוגי עבודה</label>
-              
-              {/* עיצובים בגבס */}
-              <div style={{marginBottom: '20px'}}>
-                <label className="checkbox-item" style={{fontWeight: 'bold'}}>
-                  <input
-                    type="checkbox"
-                    checked={serviceDetails.work_types?.includes('עיצובים בגבס') || false}
-                    onChange={(e) => {
-                      const current = serviceDetails.work_types || [];
-                      const newTypes = e.target.checked 
-                        ? [...current, 'עיצובים בגבס']
-                        : current.filter(t => t !== 'עיצובים בגבס');
-                      handleServiceDetailsChange('work_types', newTypes);
-                    }}
-                  />
-                  עיצובים בגבס
-                </label>
-                
-                {serviceDetails.work_types?.includes('עיצובים בגבס') && (
-                  <div style={{marginRight: '30px', marginTop: '10px'}}>
-                    <div className="checkbox-group" data-field="design_types">
-                      {['נישות גבס', 'מזנון גבס', 'ספריות גבס', 'כוורות גבס', 'תאורה נסתרת בגבס', 'קרניז גבס מעוגל', 'קשתות גבס', 'תקרה צפה', 'קיר צף'].map(type => (
-                        <label key={type} className="checkbox-item">
-                          <input
-                            type="checkbox"
-                            checked={serviceDetails.design_types?.includes(type) || false}
-                            onChange={(e) => {
-                              const current = serviceDetails.design_types || [];
-                              const newTypes = e.target.checked 
-                                ? [...current, type]
-                                : current.filter(t => t !== type);
-                              handleServiceDetailsChange('design_types', newTypes);
-                            }}
-                          />
-                          {type}
-                        </label>
-                      ))}
-                    </div>
-                    {errors['serviceDetails.design_types'] && <span className="error-text">{errors['serviceDetails.design_types']}</span>}
-                  </div>
-                )}
-              </div>
-
-              {/* עבודות גבס */}
-              <div style={{marginBottom: '20px'}}>
-                <label className="checkbox-item" style={{fontWeight: 'bold'}}>
-                  <input
-                    type="checkbox"
-                    checked={serviceDetails.work_types?.includes('עבודות גבס') || false}
-                    onChange={(e) => {
-                      const current = serviceDetails.work_types || [];
-                      const newTypes = e.target.checked 
-                        ? [...current, 'עבודות גבס']
-                        : current.filter(t => t !== 'עבודות גבס');
-                      handleServiceDetailsChange('work_types', newTypes);
-                    }}
-                  />
-                  עבודות גבס
-                </label>
-                
-                {serviceDetails.work_types?.includes('עבודות גבס') && (
-                  <div style={{marginRight: '30px', marginTop: '10px'}}>
-                    <div className="checkbox-group" data-field="construction_types">
-                      {['בניית קירות גבס', 'בניית תקרות גבס', 'בניית מדפי גבס', 'הנמכת תקרה למזגן', 'חיפוי גבס לצנרת', 'בניית סינר\\קרניז גבס', 'בידוד אקוסטי'].map(type => (
-                        <label key={type} className="checkbox-item">
-                          <input
-                            type="checkbox"
-                            checked={serviceDetails.construction_types?.includes(type) || false}
-                            onChange={(e) => {
-                              const current = serviceDetails.construction_types || [];
-                              const newTypes = e.target.checked 
-                                ? [...current, type]
-                                : current.filter(t => t !== type);
-                              handleServiceDetailsChange('construction_types', newTypes);
-                            }}
-                          />
-                          {type}
-                        </label>
-                      ))}
-                    </div>
-                    {errors['serviceDetails.construction_types'] && <span className="error-text">{errors['serviceDetails.construction_types']}</span>}
-                  </div>
-                )}
-              </div>
-
-              {errors['serviceDetails.work_types'] && <span className="error-text">{errors['serviceDetails.work_types']}</span>}
-            </div>
-          </div>
-        </div>
-      );
-
-    case 'carpentry':
-      return (
-        <div className="service-details-form">
-          <h3>פרטי שירות נגרות</h3>
-          
-          <div className="form-section">
-            <h4>שדות חובה</h4>
-            
-            <div className="input-group">
-              <label>גיל</label>
-              <input
-                type="number"
-                value={serviceDetails.age || ''}
-                onChange={(e) => handleServiceDetailsChange('age', e.target.value)}
-                className={`standard-input ${errors['serviceDetails.age'] ? 'error' : ''}`}
-                data-field="age"
-                min="18"
-              />
-              {errors['serviceDetails.age'] && <span className="error-text">{errors['serviceDetails.age']}</span>}
-            </div>
-
-            <div className="input-group">
-              <label>שנות ניסיון</label>
-              <input
-                type="number"
-                value={serviceDetails.experience || ''}
-                onChange={(e) => handleServiceDetailsChange('experience', e.target.value)}
-                className={`standard-input ${errors['serviceDetails.experience'] ? 'error' : ''}`}
-                data-field="experience"
-              />
-              {errors['serviceDetails.experience'] && <span className="error-text">{errors['serviceDetails.experience']}</span>}
-            </div>
-
-            <div className="input-group">
-              <label>ימי זמינות</label>
-              <div className="checkbox-group" data-field="availability_days">
-                {['ראשון', 'שני', 'שלישי', 'רביעי', 'חמישי', 'שישי', 'כל השבוע'].map(day => (
-                  <label key={day} className="checkbox-item">
-                    <input
-                      type="checkbox"
-                      checked={serviceDetails.availability_days?.includes(day) || false}
-                      onChange={() => handleExclusiveCheckbox('availability_days', day, 'כל השבוע', ['ראשון', 'שני', 'שלישי', 'רביעי', 'חמישי', 'שישי'])}
-                    />
-                    {day}
-                  </label>
-                ))}
-              </div>
-              {errors['serviceDetails.availability_days'] && <span className="error-text">{errors['serviceDetails.availability_days']}</span>}
-            </div>
-
-            <div className="input-group">
-              <label>שעות זמינות</label>
-              <div className="checkbox-group" data-field="availability_hours">
-                {['בוקר', 'אחר הצהריים', 'ערב', 'הכל'].map(hour => (
-                  <label key={hour} className="checkbox-item">
-                    <input
-                      type="checkbox"
-                      checked={serviceDetails.availability_hours?.includes(hour) || false}
-                      onChange={() => handleExclusiveCheckbox('availability_hours', hour, 'הכל', ['בוקר', 'אחר הצהריים', 'ערב'])}
-                    />
-                    {hour}
-                  </label>
-                ))}
-              </div>
-              {errors['serviceDetails.availability_hours'] && <span className="error-text">{errors['serviceDetails.availability_hours']}</span>}
-            </div>
-
-            <div className="input-group">
-              <label>סוגי עבודה</label>
-              
-              {/* בניית רהיטים */}
-              <div style={{marginBottom: '20px'}}>
-                <label className="checkbox-item" style={{fontWeight: 'bold'}}>
-                  <input
-                    type="checkbox"
-                    checked={serviceDetails.work_types?.includes('בניית רהיטים') || false}
-                    onChange={(e) => {
-                      const current = serviceDetails.work_types || [];
-                      const newTypes = e.target.checked 
-                        ? [...current, 'בניית רהיטים']
-                        : current.filter(t => t !== 'בניית רהיטים');
-                      handleServiceDetailsChange('work_types', newTypes);
-                    }}
-                  />
-                  בניית רהיטים
-                </label>
-                
-                {serviceDetails.work_types?.includes('בניית רהיטים') && (
-                  <div style={{marginRight: '30px', marginTop: '10px'}}>
-                    <div className="checkbox-group" data-field="furniture_building_types">
-                      {['בניית ארונות קיר', 'בניית ארונות הזזה', 'בניית ארונות אמבטיה', 'בניית חדר שינה', 'בניית שולחן', 'בניית כסאות', 'בניית מזנון', 'בניית ספריה', 'בניית רהיטים ייחודים', 'בניית מדפים', 'בניית חדר ארונות', 'בניית מיטה מעץ'].map(type => (
-                        <label key={type} className="checkbox-item">
-                          <input
-                            type="checkbox"
-                            checked={serviceDetails.furniture_building_types?.includes(type) || false}
-                            onChange={(e) => {
-                              const current = serviceDetails.furniture_building_types || [];
-                              const newTypes = e.target.checked 
-                                ? [...current, type]
-                                : current.filter(t => t !== type);
-                              handleServiceDetailsChange('furniture_building_types', newTypes);
-                            }}
-                          />
-                          {type}
-                        </label>
-                      ))}
-                    </div>
-                    {errors['serviceDetails.furniture_building_types'] && <span className="error-text">{errors['serviceDetails.furniture_building_types']}</span>}
-                  </div>
-                )}
-              </div>
-
-              {/* תיקון רהיטים */}
-              <div style={{marginBottom: '20px'}}>
-                <label className="checkbox-item" style={{fontWeight: 'bold'}}>
-                  <input
-                    type="checkbox"
-                    checked={serviceDetails.work_types?.includes('תיקון רהיטים') || false}
-                    onChange={(e) => {
-                      const current = serviceDetails.work_types || [];
-                      const newTypes = e.target.checked 
-                        ? [...current, 'תיקון רהיטים']
-                        : current.filter(t => t !== 'תיקון רהיטים');
-                      handleServiceDetailsChange('work_types', newTypes);
-                    }}
-                  />
-                  תיקון רהיטים
-                </label>
-                
-                {serviceDetails.work_types?.includes('תיקון רהיטים') && (
-                  <div style={{marginRight: '30px', marginTop: '10px'}}>
-                    <div className="checkbox-group" data-field="furniture_repair_types">
-                      {['תיקון ארונות קיר', 'תיקון שולחן', 'תיקון כסאות', 'תיקון ארונות הזזה', 'תיקון ארונות אמבטיה', 'תיקון חדר שינה', 'תיקון מזנון', 'תיקון ספרייה', 'תיקון רהיטים אחרים'].map(type => (
-                        <label key={type} className="checkbox-item">
-                          <input
-                            type="checkbox"
-                            checked={serviceDetails.furniture_repair_types?.includes(type) || false}
-                            onChange={(e) => {
-                              const current = serviceDetails.furniture_repair_types || [];
-                              const newTypes = e.target.checked 
-                                ? [...current, type]
-                                : current.filter(t => t !== type);
-                              handleServiceDetailsChange('furniture_repair_types', newTypes);
-                            }}
-                          />
-                          {type}
-                        </label>
-                      ))}
-                    </div>
-                    {errors['serviceDetails.furniture_repair_types'] && <span className="error-text">{errors['serviceDetails.furniture_repair_types']}</span>}
-                  </div>
-                )}
-              </div>
-
-              {/* עבודות נגרות אחרות */}
-              <div style={{marginBottom: '20px'}}>
-                <label className="checkbox-item" style={{fontWeight: 'bold'}}>
-                  <input
-                    type="checkbox"
-                    checked={serviceDetails.work_types?.includes('עבודות נגרות אחרות') || false}
-                    onChange={(e) => {
-                      const current = serviceDetails.work_types || [];
-                      const newTypes = e.target.checked 
-                        ? [...current, 'עבודות נגרות אחרות']
-                        : current.filter(t => t !== 'עבודות נגרות אחרות');
-                      handleServiceDetailsChange('work_types', newTypes);
-                    }}
-                  />
-                  עבודות נגרות אחרות
-                </label>
-                
-                {serviceDetails.work_types?.includes('עבודות נגרות אחרות') && (
-                  <div style={{marginRight: '30px', marginTop: '10px'}}>
-                    <div className="checkbox-group" data-field="other_carpentry_types">
-                      {['חיפוי עץ לקיר', 'פירוק והרכבת רהיטים', 'תיקון ובניית דלתות', 'חידוש דלתות כניסה מעץ', 'בניית קומת גלריה', 'מדרגות עץ לבית', 'משרביות מעץ', 'בוצ\'ר עץ'].map(type => (
-                        <label key={type} className="checkbox-item">
-                          <input
-                            type="checkbox"
-                            checked={serviceDetails.other_carpentry_types?.includes(type) || false}
-                            onChange={(e) => {
-                              const current = serviceDetails.other_carpentry_types || [];
-                              const newTypes = e.target.checked 
-                                ? [...current, type]
-                                : current.filter(t => t !== type);
-                              handleServiceDetailsChange('other_carpentry_types', newTypes);
-                            }}
-                          />
-                          {type}
-                        </label>
-                      ))}
-                    </div>
-                    {errors['serviceDetails.other_carpentry_types'] && <span className="error-text">{errors['serviceDetails.other_carpentry_types']}</span>}
-                  </div>
-                )}
-              </div>
-{/* נגרות חוץ - NIVEAU 1 */}
-<div style={{marginBottom: '20px'}}>
-  <label className="checkbox-item" style={{fontWeight: 'bold'}}>
-    <input
-      type="checkbox"
-      checked={serviceDetails.work_types?.includes('נגרות חוץ') || false}
-      onChange={(e) => {
-        const current = serviceDetails.work_types || [];
-        const newTypes = e.target.checked 
-          ? [...current, 'נגרות חוץ']
-          : current.filter(t => t !== 'נגרות חוץ');
-        handleServiceDetailsChange('work_types', newTypes);
-      }}
-    />
-    נגרות חוץ
-  </label>
-  
-  {serviceDetails.work_types?.includes('נגרות חוץ') && (
-    <div style={{marginRight: '30px', marginTop: '10px'}}>
-      
-      {/* NIVEAU 2 - פרגולות */}
-      <div style={{marginBottom: '15px'}}>
-        <label className="checkbox-item" style={{fontWeight: '600'}}>
-          <input
-            type="checkbox"
-            checked={serviceDetails.outdoor_carpentry_types?.includes('פרגולות') || false}
-            onChange={(e) => {
-              const current = serviceDetails.outdoor_carpentry_types || [];
-              const newTypes = e.target.checked 
-                ? [...current, 'פרגולות']
-                : current.filter(t => t !== 'פרגולות');
-              handleServiceDetailsChange('outdoor_carpentry_types', newTypes);
-            }}
-          />
-          פרגולות
-        </label>
-        
-        {serviceDetails.outdoor_carpentry_types?.includes('פרגולות') && (
-          <div style={{marginRight: '30px', marginTop: '10px'}}>
-            <div className="checkbox-group" data-field="pergola_types">
-              {['פרגולות עץ', 'פרגולות הצללה', 'סגירת מרפסת'].map(type => (
-                <label key={type} className="checkbox-item">
-                  <input
-                    type="checkbox"
-                    checked={serviceDetails.pergola_types?.includes(type) || false}
-                    onChange={(e) => {
-                      const current = serviceDetails.pergola_types || [];
-                      const newTypes = e.target.checked 
-                        ? [...current, type]
-                        : current.filter(t => t !== type);
-                      handleServiceDetailsChange('pergola_types', newTypes);
-                    }}
-                  />
-                  {type}
-                </label>
-              ))}
-            </div>
-            {errors['serviceDetails.pergola_types'] && <span className="error-text">{errors['serviceDetails.pergola_types']}</span>}
-          </div>
-        )}
-      </div>
-
-      {/* NIVEAU 2 - דקים */}
-      <div style={{marginBottom: '15px'}}>
-        <label className="checkbox-item" style={{fontWeight: '600'}}>
-          <input
-            type="checkbox"
-            checked={serviceDetails.outdoor_carpentry_types?.includes('דקים') || false}
-            onChange={(e) => {
-              const current = serviceDetails.outdoor_carpentry_types || [];
-              const newTypes = e.target.checked 
-                ? [...current, 'דקים']
-                : current.filter(t => t !== 'דקים');
-              handleServiceDetailsChange('outdoor_carpentry_types', newTypes);
-            }}
-          />
-          דקים
-        </label>
-        
-        {serviceDetails.outdoor_carpentry_types?.includes('דקים') && (
-          <div style={{marginRight: '30px', marginTop: '10px'}}>
-            <div className="checkbox-group" data-field="deck_types">
-              {['דקים מעץ טבעי', 'דק סינטטי (קומפוזיט)', 'שיקום / חידוש דקים'].map(type => (
-                <label key={type} className="checkbox-item">
-                  <input
-                    type="checkbox"
-                    checked={serviceDetails.deck_types?.includes(type) || false}
-                    onChange={(e) => {
-                      const current = serviceDetails.deck_types || [];
-                      const newTypes = e.target.checked 
-                        ? [...current, type]
-                        : current.filter(t => t !== type);
-                      handleServiceDetailsChange('deck_types', newTypes);
-                    }}
-                  />
-                  {type}
-                </label>
-              ))}
-            </div>
-            {errors['serviceDetails.deck_types'] && <span className="error-text">{errors['serviceDetails.deck_types']}</span>}
-          </div>
-        )}
-      </div>
-
-      {/* NIVEAU 2 - גדרות ומחיצות עץ */}
-      <div style={{marginBottom: '15px'}}>
-        <label className="checkbox-item" style={{fontWeight: '600'}}>
-          <input
-            type="checkbox"
-            checked={serviceDetails.outdoor_carpentry_types?.includes('גדרות ומחיצות עץ') || false}
-            onChange={(e) => {
-              const current = serviceDetails.outdoor_carpentry_types || [];
-              const newTypes = e.target.checked 
-                ? [...current, 'גדרות ומחיצות עץ']
-                : current.filter(t => t !== 'גדרות ומחיצות עץ');
-              handleServiceDetailsChange('outdoor_carpentry_types', newTypes);
-            }}
-          />
-          גדרות ומחיצות עץ
-        </label>
-        
-        {serviceDetails.outdoor_carpentry_types?.includes('גדרות ומחיצות עץ') && (
-          <div style={{marginRight: '30px', marginTop: '10px'}}>
-            <div className="checkbox-group" data-field="fence_types">
-              {['גדרות עץ', 'מחיצות עץ לגינה', 'שערי עץ'].map(type => (
-                <label key={type} className="checkbox-item">
-                  <input
-                    type="checkbox"
-                    checked={serviceDetails.fence_types?.includes(type) || false}
-                    onChange={(e) => {
-                      const current = serviceDetails.fence_types || [];
-                      const newTypes = e.target.checked 
-                        ? [...current, type]
-                        : current.filter(t => t !== type);
-                      handleServiceDetailsChange('fence_types', newTypes);
-                    }}
-                  />
-                  {type}
-                </label>
-              ))}
-            </div>
-            {errors['serviceDetails.fence_types'] && <span className="error-text">{errors['serviceDetails.fence_types']}</span>}
-          </div>
-        )}
-      </div>
-    </div>
-  )}
-</div>
-              {errors['serviceDetails.work_types'] && <span className="error-text">{errors['serviceDetails.work_types']}</span>}
-            </div>
-          </div>
-        </div>
-      );
-
-    case 'home_organization':
-      return (
-        <div className="service-details-form">
-          <h3>פרטי שירות סידור בית</h3>
-          
-          <div className="form-section">
-            <h4>שדות חובה</h4>
-            
-            <div className="input-group">
-              <label>גיל</label>
-              <input
-                type="number"
-                value={serviceDetails.age || ''}
-                onChange={(e) => handleServiceDetailsChange('age', e.target.value)}
-                className={`standard-input ${errors['serviceDetails.age'] ? 'error' : ''}`}
-                data-field="age"
-                min="18"
-              />
-              {errors['serviceDetails.age'] && <span className="error-text">{errors['serviceDetails.age']}</span>}
-            </div>
-
-            <div className="input-group">
-              <label>שנות ניסיון</label>
-              <input
-                type="number"
-                value={serviceDetails.experience || ''}
-                onChange={(e) => handleServiceDetailsChange('experience', e.target.value)}
-                className={`standard-input ${errors['serviceDetails.experience'] ? 'error' : ''}`}
-                data-field="experience"
-              />
-              {errors['serviceDetails.experience'] && <span className="error-text">{errors['serviceDetails.experience']}</span>}
-            </div>
-
-            <div className="input-group">
-              <label>ימי זמינות</label>
-              <div className="checkbox-group" data-field="availability_days">
-                {['ראשון', 'שני', 'שלישי', 'רביעי', 'חמישי', 'שישי', 'כל השבוע'].map(day => (
-                  <label key={day} className="checkbox-item">
-                    <input
-                      type="checkbox"
-                      checked={serviceDetails.availability_days?.includes(day) || false}
-                      onChange={() => handleExclusiveCheckbox('availability_days', day, 'כל השבוע', ['ראשון', 'שני', 'שלישי', 'רביעי', 'חמישי', 'שישי'])}
-                    />
-                    {day}
-                  </label>
-                ))}
-              </div>
-              {errors['serviceDetails.availability_days'] && <span className="error-text">{errors['serviceDetails.availability_days']}</span>}
-            </div>
-
-            <div className="input-group">
-              <label>שעות זמינות</label>
-              <div className="checkbox-group" data-field="availability_hours">
-                {['בוקר', 'אחר הצהריים', 'ערב', 'הכל'].map(hour => (
-                  <label key={hour} className="checkbox-item">
-                    <input
-                      type="checkbox"
-                      checked={serviceDetails.availability_hours?.includes(hour) || false}
-                      onChange={() => handleExclusiveCheckbox('availability_hours', hour, 'הכל', ['בוקר', 'אחר הצהריים', 'ערב'])}
-                    />
-                    {hour}
-                  </label>
-                ))}
-              </div>
-              {errors['serviceDetails.availability_hours'] && <span className="error-text">{errors['serviceDetails.availability_hours']}</span>}
-            </div>
-            <div className="input-group">
-  <label>סוגי עבודה</label>
-  
-  {/* סידור כללי */}
-  <div style={{marginBottom: '20px'}}>
-    <label className="checkbox-item" style={{fontWeight: 'bold'}}>
-      <input
-        type="checkbox"
-        checked={serviceDetails.work_types?.includes('סידור כללי') || false}
-        onChange={(e) => {
-          const current = serviceDetails.work_types || [];
-          const newTypes = e.target.checked 
-            ? [...current, 'סידור כללי']
-            : current.filter(t => t !== 'סידור כללי');
-          handleServiceDetailsChange('work_types', newTypes);
-        }}
-      />
-      סידור כללי
-    </label>
-    
-    {serviceDetails.work_types?.includes('סידור כללי') && (
-      <div style={{marginRight: '30px', marginTop: '10px'}}>
-        <div className="checkbox-group" data-field="general_organization_types">
-          {['סידור בית מלא', 'סידור חדרים', 'סידור מטבח', 'סידור חדר ילדים', 'סידור חדר ארונות / ארונות בגדים', 'סידור חדר אמבטיה'].map(type => (
-            <label key={type} className="checkbox-item">
-              <input
-                type="checkbox"
-                checked={serviceDetails.general_organization_types?.includes(type) || false}
-                onChange={(e) => {
-                  const current = serviceDetails.general_organization_types || [];
-                  const newTypes = e.target.checked 
-                    ? [...current, type]
-                    : current.filter(t => t !== type);
-                  handleServiceDetailsChange('general_organization_types', newTypes);
-                }}
-              />
-              {type}
-            </label>
-          ))}
-        </div>
-        {errors['serviceDetails.general_organization_types'] && <span className="error-text">{errors['serviceDetails.general_organization_types']}</span>}
-      </div>
-    )}
-  </div>
-
-  {/* סידור + מיון */}
-  <div style={{marginBottom: '20px'}}>
-    <label className="checkbox-item" style={{fontWeight: 'bold'}}>
-      <input
-        type="checkbox"
-        checked={serviceDetails.work_types?.includes('סידור + מיון') || false}
-        onChange={(e) => {
-          const current = serviceDetails.work_types || [];
-          const newTypes = e.target.checked 
-            ? [...current, 'סידור + מיון']
-            : current.filter(t => t !== 'סידור + מיון');
-          handleServiceDetailsChange('work_types', newTypes);
-        }}
-      />
-      סידור + מיון
-    </label>
-    
-    {serviceDetails.work_types?.includes('סידור + מיון') && (
-      <div style={{marginRight: '30px', marginTop: '10px'}}>
-        <div className="checkbox-group" data-field="sorting_types">
-          {['מיון חפצים', 'מיון בגדים', 'מיון צעצועים', 'הכנת חפצים למסירה / תרומה'].map(type => (
-            <label key={type} className="checkbox-item">
-              <input
-                type="checkbox"
-                checked={serviceDetails.sorting_types?.includes(type) || false}
-                onChange={(e) => {
-                  const current = serviceDetails.sorting_types || [];
-                  const newTypes = e.target.checked 
-                    ? [...current, type]
-                    : current.filter(t => t !== type);
-                  handleServiceDetailsChange('sorting_types', newTypes);
-                }}
-              />
-              {type}
-            </label>
-          ))}
-        </div>
-        {errors['serviceDetails.sorting_types'] && <span className="error-text">{errors['serviceDetails.sorting_types']}</span>}
-      </div>
-    )}
-  </div>
-
-  {/* ארגון מקצועי */}
-  <div style={{marginBottom: '20px'}}>
-    <label className="checkbox-item" style={{fontWeight: 'bold'}}>
-      <input
-        type="checkbox"
-        checked={serviceDetails.work_types?.includes('ארגון מקצועי') || false}
-        onChange={(e) => {
-          const current = serviceDetails.work_types || [];
-          const newTypes = e.target.checked 
-            ? [...current, 'ארגון מקצועי']
-            : current.filter(t => t !== 'ארגון מקצועי');
-          handleServiceDetailsChange('work_types', newTypes);
-        }}
-      />
-      ארגון מקצועי
-    </label>
-    
-    {serviceDetails.work_types?.includes('ארגון מקצועי') && (
-      <div style={{marginRight: '30px', marginTop: '10px'}}>
-        <div className="checkbox-group" data-field="professional_organization_types">
-          {['יצירת פתרונות אחסון', 'אופטימיזציה של חללים קטנים', 'עיצוב וסידור מדפים'].map(type => (
-            <label key={type} className="checkbox-item">
-              <input
-                type="checkbox"
-                checked={serviceDetails.professional_organization_types?.includes(type) || false}
-                onChange={(e) => {
-                  const current = serviceDetails.professional_organization_types || [];
-                  const newTypes = e.target.checked 
-                    ? [...current, type]
-                    : current.filter(t => t !== type);
-                  handleServiceDetailsChange('professional_organization_types', newTypes);
-                }}
-              />
-              {type}
-            </label>
-          ))}
-        </div>
-        {errors['serviceDetails.professional_organization_types'] && <span className="error-text">{errors['serviceDetails.professional_organization_types']}</span>}
-      </div>
-    )}
-  </div>
-
-  {errors['serviceDetails.work_types'] && <span className="error-text">{errors['serviceDetails.work_types']}</span>}
-</div>
-          </div>
-        </div>
-      );
-
-     case 'event_entertainment':
-      return (
-        <div className="service-details-form">
-          <h3>פרטי שירות הפעלות ואטרקציות לאירועים</h3>
-          
-          <div className="form-section">
-            <h4>שדות חובה</h4>
-            
-            <div className="input-group">
-              <label>גיל</label>
-              <input
-                type="number"
-                value={serviceDetails.age || ''}
-                onChange={(e) => handleServiceDetailsChange('age', e.target.value)}
-                className={`standard-input ${errors['serviceDetails.age'] ? 'error' : ''}`}
-                data-field="age"
-                min="18"
-              />
-              {errors['serviceDetails.age'] && <span className="error-text">{errors['serviceDetails.age']}</span>}
-            </div>
-
-            <div className="input-group">
-              <label>שנות ניסיון</label>
-              <input
-                type="number"
-                value={serviceDetails.experience || ''}
-                onChange={(e) => handleServiceDetailsChange('experience', e.target.value)}
-                className={`standard-input ${errors['serviceDetails.experience'] ? 'error' : ''}`}
-                data-field="experience"
-              />
-              {errors['serviceDetails.experience'] && <span className="error-text">{errors['serviceDetails.experience']}</span>}
-            </div>
-
-            <div className="input-group">
-              <label>ימי זמינות</label>
-              <div className="checkbox-group" data-field="availability_days">
-                {['ראשון', 'שני', 'שלישי', 'רביעי', 'חמישי', 'שישי', 'כל השבוע'].map(day => (
-                  <label key={day} className="checkbox-item">
-                    <input
-                      type="checkbox"
-                      checked={serviceDetails.availability_days?.includes(day) || false}
-                      onChange={() => handleExclusiveCheckbox('availability_days', day, 'כל השבוע', ['ראשון', 'שני', 'שלישי', 'רביעי', 'חמישי', 'שישי'])}
-                    />
-                    {day}
-                  </label>
-                ))}
-              </div>
-              {errors['serviceDetails.availability_days'] && <span className="error-text">{errors['serviceDetails.availability_days']}</span>}
-            </div>
-
-            <div className="input-group">
-              <label>שעות זמינות</label>
-              <div className="checkbox-group" data-field="availability_hours">
-                {['בוקר', 'אחר הצהריים', 'ערב', 'הכל'].map(hour => (
-                  <label key={hour} className="checkbox-item">
-                    <input
-                      type="checkbox"
-                      checked={serviceDetails.availability_hours?.includes(hour) || false}
-                      onChange={() => handleExclusiveCheckbox('availability_hours', hour, 'הכל', ['בוקר', 'אחר הצהריים', 'ערב'])}
-                    />
-                    {hour}
-                  </label>
-                ))}
-              </div>
-              {errors['serviceDetails.availability_hours'] && <span className="error-text">{errors['serviceDetails.availability_hours']}</span>}
-            </div>
-
-            <div className="input-group">
-              <label>סוגי עבודה</label>
-              
-              {/* השכרת ציוד לאירועים */}
-              <div style={{marginBottom: '20px'}}>
-                <label className="checkbox-item" style={{fontWeight: 'bold'}}>
-                  <input
-                    type="checkbox"
-                    checked={serviceDetails.work_types?.includes('השכרת ציוד לאירועים') || false}
-                    onChange={(e) => {
-                      const current = serviceDetails.work_types || [];
-                      const newTypes = e.target.checked 
-                        ? [...current, 'השכרת ציוד לאירועים']
-                        : current.filter(t => t !== 'השכרת ציוד לאירועים');
-                      handleServiceDetailsChange('work_types', newTypes);
-                    }}
-                  />
-                  השכרת ציוד לאירועים
-                </label>
-                
-                {serviceDetails.work_types?.includes('השכרת ציוד לאירועים') && (
-                  <div style={{marginRight: '30px', marginTop: '10px'}}>
-                    <div className="checkbox-group" data-field="equipment_rental_types">
-                      
-                      {/* 🍿 מכונות מזון */}
-                      <div style={{marginBottom: '15px'}}>
-                        <label className="checkbox-item" style={{fontWeight: '600'}}>
-                          <input
-                            type="checkbox"
-                            checked={serviceDetails.equipment_rental_types?.includes('🍿 מכונות מזון') || false}
-                            onChange={(e) => {
-                              const current = serviceDetails.equipment_rental_types || [];
-                              const newTypes = e.target.checked 
-                                ? [...current, '🍿 מכונות מזון']
-                                : current.filter(t => t !== '🍿 מכונות מזון');
-                              handleServiceDetailsChange('equipment_rental_types', newTypes);
-                            }}
-                          />
-                          🍿 מכונות מזון
-                        </label>
-                        
-                        {serviceDetails.equipment_rental_types?.includes('🍿 מכונות מזון') && (
-                          <div style={{marginRight: '30px', marginTop: '8px'}}>
-                            <div className="checkbox-group" data-field="food_machine_types">
-                              {['מכונת פופקורן', 'מכונת סוכר-בורי', 'מכונת ברד', 'מכונת וופל בלגי', 'מכונת גרניטה וקפה בר', 'מכונת גלידה אמריקאית', 'מכונת מילקשייק', 'מסחטת מיצים טריים', 'מכונת נקניקיות', 'מחבת קרפים', 'מזרקת שוקולד'].map(type => (
-                                <label key={type} className="checkbox-item">
-                                  <input
-                                    type="checkbox"
-                                    checked={serviceDetails.food_machine_types?.includes(type) || false}
-                                    onChange={(e) => {
-                                      const current = serviceDetails.food_machine_types || [];
-                                      const newTypes = e.target.checked 
-                                        ? [...current, type]
-                                        : current.filter(t => t !== type);
-                                      handleServiceDetailsChange('food_machine_types', newTypes);
-                                    }}
-                                  />
-                                  {type}
-                                </label>
-                              ))}
-                            </div>
-                            {errors['serviceDetails.food_machine_types'] && <span className="error-text">{errors['serviceDetails.food_machine_types']}</span>}
-                          </div>
-                        )}
-                      </div>
-
-                      {/* 🎪 השכרת מתנפחים ומשחקים */}
-                      <div style={{marginBottom: '15px'}}>
-                        <label className="checkbox-item" style={{fontWeight: '600'}}>
-                          <input
-                            type="checkbox"
-                            checked={serviceDetails.equipment_rental_types?.includes('🎪 השכרת מתנפחים ומשחקים') || false}
-                            onChange={(e) => {
-                              const current = serviceDetails.equipment_rental_types || [];
-                              const newTypes = e.target.checked 
-                                ? [...current, '🎪 השכרת מתנפחים ומשחקים']
-                                : current.filter(t => t !== '🎪 השכרת מתנפחים ומשחקים');
-                              handleServiceDetailsChange('equipment_rental_types', newTypes);
-                            }}
-                          />
-                          🎪 השכרת מתנפחים ומשחקים
-                        </label>
-                        
-                        {serviceDetails.equipment_rental_types?.includes('🎪 השכרת מתנפחים ומשחקים') && (
-                          <div style={{marginRight: '30px', marginTop: '8px'}}>
-                            <div className="checkbox-group" data-field="inflatable_game_types">
-                              {['מתנפחים', 'ג\'ימבורי', 'עמדות משחק'].map(type => (
-                                <label key={type} className="checkbox-item">
-                                  <input
-                                    type="checkbox"
-                                    checked={serviceDetails.inflatable_game_types?.includes(type) || false}
-                                    onChange={(e) => {
-                                      const current = serviceDetails.inflatable_game_types || [];
-                                      const newTypes = e.target.checked 
-                                        ? [...current, type]
-                                        : current.filter(t => t !== type);
-                                      handleServiceDetailsChange('inflatable_game_types', newTypes);
-                                    }}
-                                  />
-                                  {type}
-                                </label>
-                              ))}
-                            </div>
-                            {errors['serviceDetails.inflatable_game_types'] && <span className="error-text">{errors['serviceDetails.inflatable_game_types']}</span>}
-                          </div>
-                        )}
-                      </div>
-
-                      {/* 💨 מכונות אפקטים להשכרה */}
-                      <div style={{marginBottom: '15px'}}>
-                        <label className="checkbox-item" style={{fontWeight: '600'}}>
-                          <input
-                            type="checkbox"
-                            checked={serviceDetails.equipment_rental_types?.includes('💨 מכונות אפקטים להשכרה') || false}
-                            onChange={(e) => {
-                              const current = serviceDetails.equipment_rental_types || [];
-                              const newTypes = e.target.checked 
-                                ? [...current, '💨 מכונות אפקטים להשכרה']
-                                : current.filter(t => t !== '💨 מכונות אפקטים להשכרה');
-                              handleServiceDetailsChange('equipment_rental_types', newTypes);
-                            }}
-                          />
-                          💨 מכונות אפקטים להשכרה
-                        </label>
-                        
-                        {serviceDetails.equipment_rental_types?.includes('💨 מכונות אפקטים להשכרה') && (
-                          <div style={{marginRight: '30px', marginTop: '8px'}}>
-                            <div className="checkbox-group" data-field="effect_machine_types">
-                              {['מכונת עשן', 'מכונת שלג', 'מכונת בועות'].map(type => (
-                                <label key={type} className="checkbox-item">
-                                  <input
-                                    type="checkbox"
-                                    checked={serviceDetails.effect_machine_types?.includes(type) || false}
-                                    onChange={(e) => {
-                                      const current = serviceDetails.effect_machine_types || [];
-                                      const newTypes = e.target.checked 
-                                        ? [...current, type]
-                                        : current.filter(t => t !== type);
-                                      handleServiceDetailsChange('effect_machine_types', newTypes);
-                                    }}
-                                  />
-                                  {type}
-                                </label>
-                              ))}
-                            </div>
-                            {errors['serviceDetails.effect_machine_types'] && <span className="error-text">{errors['serviceDetails.effect_machine_types']}</span>}
-                          </div>
-                        )}
-                      </div>
-
-                    </div>
-                    {errors['serviceDetails.equipment_rental_types'] && <span className="error-text">{errors['serviceDetails.equipment_rental_types']}</span>}
-                  </div>
-                )}
-              </div>
-
-              {/* סוגי ההפעלה */}
-              <div style={{marginBottom: '20px'}}>
-                <label className="checkbox-item" style={{fontWeight: 'bold'}}>
-                  <input
-                    type="checkbox"
-                    checked={serviceDetails.work_types?.includes('סוגי ההפעלה') || false}
-                    onChange={(e) => {
-                      const current = serviceDetails.work_types || [];
-                      const newTypes = e.target.checked 
-                        ? [...current, 'סוגי ההפעלה']
-                        : current.filter(t => t !== 'סוגי ההפעלה');
-                      handleServiceDetailsChange('work_types', newTypes);
-                    }}
-                  />
-                  סוגי ההפעלה
-                </label>
-                
-                {serviceDetails.work_types?.includes('סוגי ההפעלה') && (
-                  <div style={{marginRight: '30px', marginTop: '10px'}}>
-                    <div className="checkbox-group" data-field="entertainment_types">
-                      {['קוסם ילדים', 'ליצן ילדים', 'בלוני צורות', 'הפרחת בלונים / ניפוח בלונים במקום', 'דמויות ותחפושות', 'שעשועונים ומשחקי קבוצה', 'מופע בועות סבון', 'הפעלה מוזיקלית / ריקודים'].map(type => (
-                        <label key={type} className="checkbox-item">
-                          <input
-                            type="checkbox"
-                            checked={serviceDetails.entertainment_types?.includes(type) || false}
-                            onChange={(e) => {
-                              const current = serviceDetails.entertainment_types || [];
-                              const newTypes = e.target.checked 
-                                ? [...current, type]
-                                : current.filter(t => t !== type);
-                              handleServiceDetailsChange('entertainment_types', newTypes);
-                            }}
-                          />
-                          {type}
-                        </label>
-                      ))}
-                    </div>
-                    {errors['serviceDetails.entertainment_types'] && <span className="error-text">{errors['serviceDetails.entertainment_types']}</span>}
-                  </div>
-                )}
-              </div>
-
-              {/* אחר */}
-              <div style={{marginBottom: '20px'}}>
-                <label className="checkbox-item" style={{fontWeight: 'bold'}}>
-                  <input
-                    type="checkbox"
-                    checked={serviceDetails.work_types?.includes('אחר') || false}
-                    onChange={(e) => {
-                      const current = serviceDetails.work_types || [];
-                      const newTypes = e.target.checked 
-                        ? [...current, 'אחר']
-                        : current.filter(t => t !== 'אחר');
-                      handleServiceDetailsChange('work_types', newTypes);
-                    }}
-                  />
-                  אחר
-                </label>
-                
-                {serviceDetails.work_types?.includes('אחר') && (
-                  <div style={{marginRight: '30px', marginTop: '10px'}}>
-                    <div className="checkbox-group" data-field="other_types">
-                      {['איפור פנים מקצועי', 'בלוני קשת', 'צילום מגנטים'].map(type => (
-                        <label key={type} className="checkbox-item">
-                          <input
-                            type="checkbox"
-                            checked={serviceDetails.other_types?.includes(type) || false}
-                            onChange={(e) => {
-                              const current = serviceDetails.other_types || [];
-                              const newTypes = e.target.checked 
-                                ? [...current, type]
-                                : current.filter(t => t !== type);
-                              handleServiceDetailsChange('other_types', newTypes);
-                            }}
-                          />
-                          {type}
-                        </label>
-                      ))}
-                    </div>
-                    {errors['serviceDetails.other_types'] && <span className="error-text">{errors['serviceDetails.other_types']}</span>}
-                  </div>
-                )}
-              </div>
-
-              {errors['serviceDetails.work_types'] && <span className="error-text">{errors['serviceDetails.work_types']}</span>}
-            </div>
-          </div>
-        </div>
-      ); 
-case 'private_chef':
-      return (
-        <div className="service-details-form">
-          <h3>פרטי שירות שף פרטי</h3>
-          
-          <div className="form-section">
-            <h4>שדות חובה</h4>
-            
-            <div className="input-group">
-              <label>גיל</label>
-              <input
-                type="number"
-                value={serviceDetails.age || ''}
-                onChange={(e) => handleServiceDetailsChange('age', e.target.value)}
-                className={`standard-input ${errors['serviceDetails.age'] ? 'error' : ''}`}
-                data-field="age"
-                min="18"
-              />
-              {errors['serviceDetails.age'] && <span className="error-text">{errors['serviceDetails.age']}</span>}
-            </div>
-
-            <div className="input-group">
-              <label>שנות ניסיון</label>
-              <input
-                type="number"
-                value={serviceDetails.experience || ''}
-                onChange={(e) => handleServiceDetailsChange('experience', e.target.value)}
-                className={`standard-input ${errors['serviceDetails.experience'] ? 'error' : ''}`}
-                data-field="experience"
-              />
-              {errors['serviceDetails.experience'] && <span className="error-text">{errors['serviceDetails.experience']}</span>}
-            </div>
-
-            <div className="input-group">
-              <label>ימי זמינות</label>
-              <div className="checkbox-group" data-field="availability_days">
-                {['ראשון', 'שני', 'שלישי', 'רביעי', 'חמישי', 'שישי', 'כל השבוע'].map(day => (
-                  <label key={day} className="checkbox-item">
-                    <input
-                      type="checkbox"
-                      checked={serviceDetails.availability_days?.includes(day) || false}
-                      onChange={() => handleExclusiveCheckbox('availability_days', day, 'כל השבוע', ['ראשון', 'שני', 'שלישי', 'רביעי', 'חמישי', 'שישי'])}
-                    />
-                    {day}
-                  </label>
-                ))}
-              </div>
-              {errors['serviceDetails.availability_days'] && <span className="error-text">{errors['serviceDetails.availability_days']}</span>}
-            </div>
-
-            <div className="input-group">
-              <label>שעות זמינות</label>
-              <div className="checkbox-group" data-field="availability_hours">
-                {['בוקר', 'אחר הצהריים', 'ערב', 'הכל'].map(hour => (
-                  <label key={hour} className="checkbox-item">
-                    <input
-                      type="checkbox"
-                      checked={serviceDetails.availability_hours?.includes(hour) || false}
-                      onChange={() => handleExclusiveCheckbox('availability_hours', hour, 'הכל', ['בוקר', 'אחר הצהריים', 'ערב'])}
-                    />
-                    {hour}
-                  </label>
-                ))}
-              </div>
-              {errors['serviceDetails.availability_hours'] && <span className="error-text">{errors['serviceDetails.availability_hours']}</span>}
-            </div>
-
-            <div className="input-group">
-              <label>סוגי עבודה</label>
-              
-              {/* סוג המטבח */}
-              <div style={{marginBottom: '20px'}}>
-                <label className="checkbox-item" style={{fontWeight: 'bold'}}>
-                  <input
-                    type="checkbox"
-                    checked={serviceDetails.work_types?.includes('סוג המטבח') || false}
-                    onChange={(e) => {
-                      const current = serviceDetails.work_types || [];
-                      const newTypes = e.target.checked 
-                        ? [...current, 'סוג המטבח']
-                        : current.filter(t => t !== 'סוג המטבח');
-                      handleServiceDetailsChange('work_types', newTypes);
-                    }}
-                  />
-                  סוג המטבח
-                </label>
-                
-                {serviceDetails.work_types?.includes('סוג המטבח') && (
-                  <div style={{marginRight: '30px', marginTop: '10px'}}>
-                    <div className="checkbox-group" data-field="cuisine_types">
-                      {['פיצות', 'סושי', 'סלטים', 'אסייתי', 'פסטות', 'בשרי', 'טבעוני / צמחוני', 'לא גלוטן', 'קינוחים'].map(type => (
-                        <label key={type} className="checkbox-item">
-                          <input
-                            type="checkbox"
-                            checked={serviceDetails.cuisine_types?.includes(type) || false}
-                            onChange={(e) => {
-                              const current = serviceDetails.cuisine_types || [];
-                              const newTypes = e.target.checked 
-                                ? [...current, type]
-                                : current.filter(t => t !== type);
-                              handleServiceDetailsChange('cuisine_types', newTypes);
-                            }}
-                          />
-                          {type}
-                        </label>
-                      ))}
-                    </div>
-                    {errors['serviceDetails.cuisine_types'] && <span className="error-text">{errors['serviceDetails.cuisine_types']}</span>}
-                  </div>
-                )}
-              </div>
-
-              {/* כשרות */}
-              <div style={{marginBottom: '20px'}}>
-                <label className="checkbox-item" style={{fontWeight: 'bold'}}>
-                  <input
-                    type="checkbox"
-                    checked={serviceDetails.work_types?.includes('כשרות') || false}
-                    onChange={(e) => {
-                      const current = serviceDetails.work_types || [];
-                      const newTypes = e.target.checked 
-                        ? [...current, 'כשרות']
-                        : current.filter(t => t !== 'כשרות');
-                      handleServiceDetailsChange('work_types', newTypes);
-                    }}
-                  />
-                  כשרות
-                </label>
-                
-                {serviceDetails.work_types?.includes('כשרות') && (
-                  <div style={{marginRight: '30px', marginTop: '10px'}}>
-                    <div className="checkbox-group" data-field="kosher_types">
-                      {[
-                        'בד"ץ העדה החרדית',
-                        'בד"ץ בית יוסף',
-                        'בד"ץ יורה דעה (ר׳ שלמה מחפוד)',
-                        'בד"ץ מחזיקי הדת – בעלז',
-                        'בד"ץ שארית ישראל',
-                        'בד"ץ נתיבות כשרות',
-                        'בד"ץ חוג חתם סופר בני ברק',
-                        'בד"ץ חוג חתם סופר פ״ת',
-                        'בד"ץ מקווה ישראל',
-                        'בד"ץ רבני צפת',
-                        'כשרות הרב לנדא',
-                        'כשרות הרב רובין'
-                      ].map(type => (
-                        <label key={type} className="checkbox-item">
-                          <input
-                            type="checkbox"
-                            checked={serviceDetails.kosher_types?.includes(type) || false}
-                            onChange={(e) => {
-                              const current = serviceDetails.kosher_types || [];
-                              const newTypes = e.target.checked 
-                                ? [...current, type]
-                                : current.filter(t => t !== type);
-                              handleServiceDetailsChange('kosher_types', newTypes);
-                            }}
-                          />
-                          {type}
-                        </label>
-                      ))}
-                    </div>
-                    {errors['serviceDetails.kosher_types'] && <span className="error-text">{errors['serviceDetails.kosher_types']}</span>}
-                  </div>
-                )}
-              </div>
-
-              {errors['serviceDetails.work_types'] && <span className="error-text">{errors['serviceDetails.work_types']}</span>}
-            </div>
-          </div>
-        </div>
-      );
-case 'painting':
-      return (
-        <div className="service-details-form">
-          <h3>פרטי שירות עבודות צבע</h3>
-          
-          <div className="form-section">
-            <h4>שדות חובה</h4>
-            
-            <div className="input-group">
-              <label>גיל</label>
-              <input
-                type="number"
-                value={serviceDetails.age || ''}
-                onChange={(e) => handleServiceDetailsChange('age', e.target.value)}
-                className={`standard-input ${errors['serviceDetails.age'] ? 'error' : ''}`}
-                data-field="age"
-                min="18"
-              />
-              {errors['serviceDetails.age'] && <span className="error-text">{errors['serviceDetails.age']}</span>}
-            </div>
-
-            <div className="input-group">
-              <label>שנות ניסיון</label>
-              <input
-                type="number"
-                value={serviceDetails.experience || ''}
-                onChange={(e) => handleServiceDetailsChange('experience', e.target.value)}
-                className={`standard-input ${errors['serviceDetails.experience'] ? 'error' : ''}`}
-                data-field="experience"
-              />
-              {errors['serviceDetails.experience'] && <span className="error-text">{errors['serviceDetails.experience']}</span>}
-            </div>
-
-            <div className="input-group">
-              <label>ימי זמינות</label>
-              <div className="checkbox-group" data-field="availability_days">
-                {['ראשון', 'שני', 'שלישי', 'רביעי', 'חמישי', 'שישי', 'כל השבוע'].map(day => (
-                  <label key={day} className="checkbox-item">
-                    <input
-                      type="checkbox"
-                      checked={serviceDetails.availability_days?.includes(day) || false}
-                      onChange={() => handleExclusiveCheckbox('availability_days', day, 'כל השבוע', ['ראשון', 'שני', 'שלישי', 'רביעי', 'חמישי', 'שישי'])}
-                    />
-                    {day}
-                  </label>
-                ))}
-              </div>
-              {errors['serviceDetails.availability_days'] && <span className="error-text">{errors['serviceDetails.availability_days']}</span>}
-            </div>
-
-            <div className="input-group">
-              <label>שעות זמינות</label>
-              <div className="checkbox-group" data-field="availability_hours">
-                {['בוקר', 'אחר הצהריים', 'ערב', 'הכל'].map(hour => (
-                  <label key={hour} className="checkbox-item">
-                    <input
-                      type="checkbox"
-                      checked={serviceDetails.availability_hours?.includes(hour) || false}
-                      onChange={() => handleExclusiveCheckbox('availability_hours', hour, 'הכל', ['בוקר', 'אחר הצהריים', 'ערב'])}
-                    />
-                    {hour}
-                  </label>
-                ))}
-              </div>
-              {errors['serviceDetails.availability_hours'] && <span className="error-text">{errors['serviceDetails.availability_hours']}</span>}
-            </div>
-
-            <div className="input-group">
-              <label>סוגי עבודה</label>
-              <div className="checkbox-group" data-field="work_types">
-                {['צביעה כללית של דירה', 'תיקוני קירות – חורים, סדקים, שפכטל', 'החלקת קירות (שפכטל מלא)', 'תיקון רטיבות / עובש', 'קילופי צבע ישן', 'צביעת אפקטים – בטון, משי, אומבר', 'צביעת קיר דקורטיבי / Accent Wall', 'טקסטורות מיוחדות'].map(type => (
-                  <label key={type} className="checkbox-item">
-                    <input
-                      type="checkbox"
-                      checked={serviceDetails.work_types?.includes(type) || false}
-                      onChange={(e) => {
-                        const current = serviceDetails.work_types || [];
-                        const newTypes = e.target.checked 
-                          ? [...current, type]
-                          : current.filter(t => t !== type);
-                        handleServiceDetailsChange('work_types', newTypes);
-                      }}
-                    />
-                    {type}
-                  </label>
-                ))}
-              </div>
-              {errors['serviceDetails.work_types'] && <span className="error-text">{errors['serviceDetails.work_types']}</span>}
-            </div>
-          </div>
-        </div>
-      );
-    case 'waterproofing':
-  return (
-    <div className="service-details-form">
-      <h3>פרטי שירות איטום</h3>
-      
-      <div className="form-section">
-        <h4>שדות חובה</h4>
-        
-        <div className="input-group">
-          <label>גיל</label>
-          <input
-            type="number"
-            value={serviceDetails.age || ''}
-            onChange={(e) => handleServiceDetailsChange('age', e.target.value)}
-            className={`standard-input ${errors['serviceDetails.age'] ? 'error' : ''}`}
-            data-field="age"
-            min="18"
-          />
-          {errors['serviceDetails.age'] && <span className="error-text">{errors['serviceDetails.age']}</span>}
-        </div>
-
-        <div className="input-group">
-          <label>שנות ניסיון</label>
-          <input
-            type="number"
-            value={serviceDetails.experience || ''}
-            onChange={(e) => handleServiceDetailsChange('experience', e.target.value)}
-            className={`standard-input ${errors['serviceDetails.experience'] ? 'error' : ''}`}
-            data-field="experience"
-          />
-          {errors['serviceDetails.experience'] && <span className="error-text">{errors['serviceDetails.experience']}</span>}
-        </div>
-
-        <div className="input-group">
-          <label>ימי זמינות</label>
-          <div className="checkbox-group" data-field="availability_days">
-            {['ראשון', 'שני', 'שלישי', 'רביעי', 'חמישי', 'שישי', 'כל השבוע'].map(day => (
-              <label key={day} className="checkbox-item">
-                <input
-                  type="checkbox"
-                  checked={serviceDetails.availability_days?.includes(day) || false}
-                  onChange={() => handleExclusiveCheckbox('availability_days', day, 'כל השבוע', ['ראשון', 'שני', 'שלישי', 'רביעי', 'חמישי', 'שישי'])}
-                />
-                {day}
-              </label>
-            ))}
-          </div>
-          {errors['serviceDetails.availability_days'] && <span className="error-text">{errors['serviceDetails.availability_days']}</span>}
-        </div>
-
-        <div className="input-group">
-          <label>שעות זמינות</label>
-          <div className="checkbox-group" data-field="availability_hours">
-            {['בוקר', 'אחר הצהריים', 'ערב', 'הכל'].map(hour => (
-              <label key={hour} className="checkbox-item">
-                <input
-                  type="checkbox"
-                  checked={serviceDetails.availability_hours?.includes(hour) || false}
-                  onChange={() => handleExclusiveCheckbox('availability_hours', hour, 'הכל', ['בוקר', 'אחר הצהריים', 'ערב'])}
-                />
-                {hour}
-              </label>
-            ))}
-          </div>
-          {errors['serviceDetails.availability_hours'] && <span className="error-text">{errors['serviceDetails.availability_hours']}</span>}
-        </div>
-
-        <div className="input-group">
-          <label>סוגי עבודה</label>
-          
-          {/* איטום גגות */}
-          <div style={{marginBottom: '20px'}}>
-            <label className="checkbox-item" style={{fontWeight: 'bold'}}>
-              <input
-                type="checkbox"
-                checked={serviceDetails.work_types?.includes('איטום גגות') || false}
-                onChange={(e) => {
-                  const current = serviceDetails.work_types || [];
-                  const newTypes = e.target.checked 
-                    ? [...current, 'איטום גגות']
-                    : current.filter(t => t !== 'איטום גגות');
-                  handleServiceDetailsChange('work_types', newTypes);
-                }}
-              />
-              איטום גגות
-            </label>
-            
-            {serviceDetails.work_types?.includes('איטום גגות') && (
-              <div style={{marginRight: '30px', marginTop: '10px'}}>
-                <div className="checkbox-group" data-field="roof_waterproofing_types">
-                  {['איטום גג ביריעות ביטומניות', 'איטום גג בזפת חמה', 'איטום גג עם פוליאוריטן', 'איטום גג רעפים', 'תחזוקת גגות – ניקיון, תיקונים קלים'].map(type => (
-                    <label key={type} className="checkbox-item">
-                      <input
-                        type="checkbox"
-                        checked={serviceDetails.roof_waterproofing_types?.includes(type) || false}
-                        onChange={(e) => {
-                          const current = serviceDetails.roof_waterproofing_types || [];
-                          const newTypes = e.target.checked 
-                            ? [...current, type]
-                            : current.filter(t => t !== type);
-                          handleServiceDetailsChange('roof_waterproofing_types', newTypes);
-                        }}
-                      />
-                      {type}
-                    </label>
-                  ))}
-                </div>
-                {errors['serviceDetails.roof_waterproofing_types'] && <span className="error-text">{errors['serviceDetails.roof_waterproofing_types']}</span>}
-              </div>
-            )}
-          </div>
-
-          {/* איטום קירות חיצוניים */}
-          <div style={{marginBottom: '20px'}}>
-            <label className="checkbox-item" style={{fontWeight: 'bold'}}>
-              <input
-                type="checkbox"
-                checked={serviceDetails.work_types?.includes('איטום קירות חיצוניים') || false}
-                onChange={(e) => {
-                  const current = serviceDetails.work_types || [];
-                  const newTypes = e.target.checked 
-                    ? [...current, 'איטום קירות חיצוניים']
-                    : current.filter(t => t !== 'איטום קירות חיצוניים');
-                  handleServiceDetailsChange('work_types', newTypes);
-                }}
-              />
-              איטום קירות חיצוניים
-            </label>
-            
-            {serviceDetails.work_types?.includes('איטום קירות חיצוניים') && (
-              <div style={{marginRight: '30px', marginTop: '10px'}}>
-                <div className="checkbox-group" data-field="wall_waterproofing_types">
-                  {['איטום קירות מפני חדירת מים', 'שיקום קירות חוץ', 'איטום סדקים בקירות', 'טיפול ברטיבות חיצונית'].map(type => (
-                    <label key={type} className="checkbox-item">
-                      <input
-                        type="checkbox"
-                        checked={serviceDetails.wall_waterproofing_types?.includes(type) || false}
-                        onChange={(e) => {
-                          const current = serviceDetails.wall_waterproofing_types || [];
-                          const newTypes = e.target.checked 
-                            ? [...current, type]
-                            : current.filter(t => t !== type);
-                          handleServiceDetailsChange('wall_waterproofing_types', newTypes);
-                        }}
-                      />
-                      {type}
-                    </label>
-                  ))}
-                </div>
-                {errors['serviceDetails.wall_waterproofing_types'] && <span className="error-text">{errors['serviceDetails.wall_waterproofing_types']}</span>}
-              </div>
-            )}
-          </div>
-
-          {/* איטום מרפסות */}
-          <div style={{marginBottom: '20px'}}>
-            <label className="checkbox-item" style={{fontWeight: 'bold'}}>
-              <input
-                type="checkbox"
-                checked={serviceDetails.work_types?.includes('איטום מרפסות') || false}
-                onChange={(e) => {
-                  const current = serviceDetails.work_types || [];
-                  const newTypes = e.target.checked 
-                    ? [...current, 'איטום מרפסות']
-                    : current.filter(t => t !== 'איטום מרפסות');
-                  handleServiceDetailsChange('work_types', newTypes);
-                }}
-              />
-              איטום מרפסות
-            </label>
-            
-            {serviceDetails.work_types?.includes('איטום מרפסות') && (
-              <div style={{marginRight: '30px', marginTop: '10px'}}>
-                <div className="checkbox-group" data-field="balcony_waterproofing_types">
-                  {['איטום מרפסת לפני ריצוף', 'איטום מרפסת קיימת (תיקון נזילות)', 'ריצוף + איטום מרפסת'].map(type => (
-                    <label key={type} className="checkbox-item">
-                      <input
-                        type="checkbox"
-                        checked={serviceDetails.balcony_waterproofing_types?.includes(type) || false}
-                        onChange={(e) => {
-                          const current = serviceDetails.balcony_waterproofing_types || [];
-                          const newTypes = e.target.checked 
-                            ? [...current, type]
-                            : current.filter(t => t !== type);
-                          handleServiceDetailsChange('balcony_waterproofing_types', newTypes);
-                        }}
-                      />
-                      {type}
-                    </label>
-                  ))}
-                </div>
-                {errors['serviceDetails.balcony_waterproofing_types'] && <span className="error-text">{errors['serviceDetails.balcony_waterproofing_types']}</span>}
-              </div>
-            )}
-          </div>
-
-          {/* איטום חדרים רטובים */}
-          <div style={{marginBottom: '20px'}}>
-            <label className="checkbox-item" style={{fontWeight: 'bold'}}>
-              <input
-                type="checkbox"
-                checked={serviceDetails.work_types?.includes('איטום חדרים רטובים') || false}
-                onChange={(e) => {
-                  const current = serviceDetails.work_types || [];
-                  const newTypes = e.target.checked 
-                    ? [...current, 'איטום חדרים רטובים']
-                    : current.filter(t => t !== 'איטום חדרים רטובים');
-                  handleServiceDetailsChange('work_types', newTypes);
-                }}
-              />
-              איטום חדרים רטובים
-            </label>
-            
-            {serviceDetails.work_types?.includes('איטום חדרים רטובים') && (
-              <div style={{marginRight: '30px', marginTop: '10px'}}>
-                <div className="checkbox-group" data-field="wet_room_waterproofing_types">
-                  {['איטום חדר אמבטיה', 'איטום מקלחת', 'איטום שירותים', 'איטום לפני שיפוץ'].map(type => (
-                    <label key={type} className="checkbox-item">
-                      <input
-                        type="checkbox"
-                        checked={serviceDetails.wet_room_waterproofing_types?.includes(type) || false}
-                        onChange={(e) => {
-                          const current = serviceDetails.wet_room_waterproofing_types || [];
-                          const newTypes = e.target.checked 
-                            ? [...current, type]
-                            : current.filter(t => t !== type);
-                          handleServiceDetailsChange('wet_room_waterproofing_types', newTypes);
-                        }}
-                      />
-                      {type}
-                    </label>
-                  ))}
-                </div>
-                {errors['serviceDetails.wet_room_waterproofing_types'] && <span className="error-text">{errors['serviceDetails.wet_room_waterproofing_types']}</span>}
-              </div>
-            )}
-          </div>
-
-          {/* איטום תת-קרקעי */}
-          <div style={{marginBottom: '20px'}}>
-            <label className="checkbox-item" style={{fontWeight: 'bold'}}>
-              <input
-                type="checkbox"
-                checked={serviceDetails.work_types?.includes('איטום תת-קרקעי') || false}
-                onChange={(e) => {
-                  const current = serviceDetails.work_types || [];
-                  const newTypes = e.target.checked 
-                    ? [...current, 'איטום תת-קרקעי']
-                    : current.filter(t => t !== 'איטום תת-קרקעי');
-                  handleServiceDetailsChange('work_types', newTypes);
-                }}
-              />
-              איטום תת-קרקעי
-            </label>
-            
-            {serviceDetails.work_types?.includes('איטום תת-קרקעי') && (
-              <div style={{marginRight: '30px', marginTop: '10px'}}>
-                <div className="checkbox-group" data-field="underground_waterproofing_types">
-                  {['איטום מרתפים', 'איטום יסודות בניין', 'איטום קירות בחדרים תת-קרקעיים'].map(type => (
-                    <label key={type} className="checkbox-item">
-                      <input
-                        type="checkbox"
-                        checked={serviceDetails.underground_waterproofing_types?.includes(type) || false}
-                        onChange={(e) => {
-                          const current = serviceDetails.underground_waterproofing_types || [];
-                          const newTypes = e.target.checked 
-                            ? [...current, type]
-                            : current.filter(t => t !== type);
-                          handleServiceDetailsChange('underground_waterproofing_types', newTypes);
-                        }}
-                      />
-                      {type}
-                    </label>
-                  ))}
-                </div>
-                {errors['serviceDetails.underground_waterproofing_types'] && <span className="error-text">{errors['serviceDetails.underground_waterproofing_types']}</span>}
-              </div>
-            )}
-          </div>
-
-          {/* בדיקות, אבחון וציוד */}
-          <div style={{marginBottom: '20px'}}>
-            <label className="checkbox-item" style={{fontWeight: 'bold'}}>
-              <input
-                type="checkbox"
-                checked={serviceDetails.work_types?.includes('בדיקות, אבחון וציוד') || false}
-                onChange={(e) => {
-                  const current = serviceDetails.work_types || [];
-                  const newTypes = e.target.checked 
-                    ? [...current, 'בדיקות, אבחון וציוד']
-                    : current.filter(t => t !== 'בדיקות, אבחון וציוד');
-                  handleServiceDetailsChange('work_types', newTypes);
-                }}
-              />
-              בדיקות, אבחון וציוד
-            </label>
-            
-            {serviceDetails.work_types?.includes('בדיקות, אבחון וציוד') && (
-              <div style={{marginRight: '30px', marginTop: '10px'}}>
-                <div className="checkbox-group" data-field="inspection_equipment_types">
-                  {['איתור נזילות', 'בדיקות רטיבות', 'צילום תרמי לאיתור נזילות'].map(type => (
-                    <label key={type} className="checkbox-item">
-                      <input
-                        type="checkbox"
-                        checked={serviceDetails.inspection_equipment_types?.includes(type) || false}
-                        onChange={(e) => {
-                          const current = serviceDetails.inspection_equipment_types || [];
-                          const newTypes = e.target.checked 
-                            ? [...current, type]
-                            : current.filter(t => t !== type);
-                          handleServiceDetailsChange('inspection_equipment_types', newTypes);
-                        }}
-                      />
-                      {type}
-                    </label>
-                  ))}
-                </div>
-                {errors['serviceDetails.inspection_equipment_types'] && <span className="error-text">{errors['serviceDetails.inspection_equipment_types']}</span>}
-              </div>
-            )}
-          </div>
-
-          {errors['serviceDetails.work_types'] && <span className="error-text">{errors['serviceDetails.work_types']}</span>}
-        </div>
-      </div>
-    </div>
-  );
-  case 'contractor':
-  return (
-    <div className="service-details-form">
-      <h3>פרטי שירות קבלן</h3>
-      
-      <div className="form-section">
-        <h4>שדות חובה</h4>
-        
-        <div className="input-group">
-          <label>גיל</label>
-          <input
-            type="number"
-            value={serviceDetails.age || ''}
-            onChange={(e) => handleServiceDetailsChange('age', e.target.value)}
-            className={`standard-input ${errors['serviceDetails.age'] ? 'error' : ''}`}
-            data-field="age"
-            min="18"
-          />
-          {errors['serviceDetails.age'] && <span className="error-text">{errors['serviceDetails.age']}</span>}
-        </div>
-
-        <div className="input-group">
-          <label>שנות ניסיון</label>
-          <input
-            type="number"
-            value={serviceDetails.experience || ''}
-            onChange={(e) => handleServiceDetailsChange('experience', e.target.value)}
-            className={`standard-input ${errors['serviceDetails.experience'] ? 'error' : ''}`}
-            data-field="experience"
-          />
-          {errors['serviceDetails.experience'] && <span className="error-text">{errors['serviceDetails.experience']}</span>}
-        </div>
-
-        <div className="input-group">
-          <label>ימי זמינות</label>
-          <div className="checkbox-group" data-field="availability_days">
-            {['ראשון', 'שני', 'שלישי', 'רביעי', 'חמישי', 'שישי', 'כל השבוע'].map(day => (
-              <label key={day} className="checkbox-item">
-                <input
-                  type="checkbox"
-                  checked={serviceDetails.availability_days?.includes(day) || false}
-                  onChange={() => handleExclusiveCheckbox('availability_days', day, 'כל השבוע', ['ראשון', 'שני', 'שלישי', 'רביעי', 'חמישי', 'שישי'])}
-                />
-                {day}
-              </label>
-            ))}
-          </div>
-          {errors['serviceDetails.availability_days'] && <span className="error-text">{errors['serviceDetails.availability_days']}</span>}
-        </div>
-
-        <div className="input-group">
-          <label>שעות זמינות</label>
-          <div className="checkbox-group" data-field="availability_hours">
-            {['בוקר', 'אחר הצהריים', 'ערב', 'הכל'].map(hour => (
-              <label key={hour} className="checkbox-item">
-                <input
-                  type="checkbox"
-                  checked={serviceDetails.availability_hours?.includes(hour) || false}
-                  onChange={() => handleExclusiveCheckbox('availability_hours', hour, 'הכל', ['בוקר', 'אחר הצהריים', 'ערב'])}
-                />
-                {hour}
-              </label>
-            ))}
-          </div>
-          {errors['serviceDetails.availability_hours'] && <span className="error-text">{errors['serviceDetails.availability_hours']}</span>}
-        </div>
-
-        <div className="input-group">
-          <label>סוגי עבודה</label>
-          
-          {/* עבודות שלד */}
-          <div style={{marginBottom: '20px'}}>
-            <label className="checkbox-item" style={{fontWeight: 'bold'}}>
-              <input
-                type="checkbox"
-                checked={serviceDetails.work_types?.includes('עבודות שלד') || false}
-                onChange={(e) => {
-                  const current = serviceDetails.work_types || [];
-                  const newTypes = e.target.checked 
-                    ? [...current, 'עבודות שלד']
-                    : current.filter(t => t !== 'עבודות שלד');
-                  handleServiceDetailsChange('work_types', newTypes);
-                }}
-              />
-              עבודות שלד
-            </label>
-            
-            {serviceDetails.work_types?.includes('עבודות שלד') && (
-              <div style={{marginRight: '30px', marginTop: '10px'}}>
-                <div className="checkbox-group" data-field="structure_work_types">
-                  {['בניית שלד', 'יציקות בטון', 'טפסנות', 'חיזוק מבנים', 'בניית קירות בלוקים', 'הריסה ובנייה מחדש'].map(type => (
-                    <label key={type} className="checkbox-item">
-                      <input
-                        type="checkbox"
-                        checked={serviceDetails.structure_work_types?.includes(type) || false}
-                        onChange={(e) => {
-                          const current = serviceDetails.structure_work_types || [];
-                          const newTypes = e.target.checked 
-                            ? [...current, type]
-                            : current.filter(t => t !== type);
-                          handleServiceDetailsChange('structure_work_types', newTypes);
-                        }}
-                      />
-                      {type}
-                    </label>
-                  ))}
-                </div>
-                {errors['serviceDetails.structure_work_types'] && <span className="error-text">{errors['serviceDetails.structure_work_types']}</span>}
-              </div>
-            )}
-          </div>
-
-          {/* שיפוצים כלליים */}
-          <div style={{marginBottom: '20px'}}>
-            <label className="checkbox-item" style={{fontWeight: 'bold'}}>
-              <input
-                type="checkbox"
-                checked={serviceDetails.work_types?.includes('שיפוצים כלליים') || false}
-                onChange={(e) => {
-                  const current = serviceDetails.work_types || [];
-                  const newTypes = e.target.checked 
-                    ? [...current, 'שיפוצים כלליים']
-                    : current.filter(t => t !== 'שיפוצים כלליים');
-                  handleServiceDetailsChange('work_types', newTypes);
-                }}
-              />
-              שיפוצים כלליים
-            </label>
-            
-            {serviceDetails.work_types?.includes('שיפוצים כלליים') && (
-              <div style={{marginRight: '30px', marginTop: '10px'}}>
-                <div className="checkbox-group" data-field="general_renovation_types">
-                  {['שיפוץ דירה מלא', 'שיפוץ חדרים', 'שיפוץ חדרי רחצה', 'שיפוץ מטבח', 'החלפת ריצוף', 'עבודות גבס', 'טיח ושפכטל', 'סגירת מרפסת', 'צביעה מקצועית', 'החלפת דלתות ומשקופים'].map(type => (
-                    <label key={type} className="checkbox-item">
-                      <input
-                        type="checkbox"
-                        checked={serviceDetails.general_renovation_types?.includes(type) || false}
-                        onChange={(e) => {
-                          const current = serviceDetails.general_renovation_types || [];
-                          const newTypes = e.target.checked 
-                            ? [...current, type]
-                            : current.filter(t => t !== type);
-                          handleServiceDetailsChange('general_renovation_types', newTypes);
-                        }}
-                      />
-                      {type}
-                    </label>
-                  ))}
-                </div>
-                {errors['serviceDetails.general_renovation_types'] && <span className="error-text">{errors['serviceDetails.general_renovation_types']}</span>}
-              </div>
-            )}
-          </div>
-
-          {/* חשמל ואינסטלציה */}
-          <div style={{marginBottom: '20px'}}>
-            <label className="checkbox-item" style={{fontWeight: 'bold'}}>
-              <input
-                type="checkbox"
-                checked={serviceDetails.work_types?.includes('חשמל ואינסטלציה') || false}
-                onChange={(e) => {
-                  const current = serviceDetails.work_types || [];
-                  const newTypes = e.target.checked 
-                    ? [...current, 'חשמל ואינסטלציה']
-                    : current.filter(t => t !== 'חשמל ואינסטלציה');
-                  handleServiceDetailsChange('work_types', newTypes);
-                }}
-              />
-              חשמל ואינסטלציה - רק אם מוסמך
-            </label>
-            
-            {serviceDetails.work_types?.includes('חשמל ואינסטלציה') && (
-              <div style={{marginRight: '30px', marginTop: '10px'}}>
-                <div className="checkbox-group" data-field="electric_plumbing_types">
-                  {['עבודות חשמל', 'החלפת לוח חשמל', 'אינסטלציה כללית', 'החלפת צנרת', 'איתור ותיקון נזילות'].map(type => (
-                    <label key={type} className="checkbox-item">
-                      <input
-                        type="checkbox"
-                        checked={serviceDetails.electric_plumbing_types?.includes(type) || false}
-                        onChange={(e) => {
-                          const current = serviceDetails.electric_plumbing_types || [];
-                          const newTypes = e.target.checked 
-                            ? [...current, type]
-                            : current.filter(t => t !== type);
-                          handleServiceDetailsChange('electric_plumbing_types', newTypes);
-                        }}
-                      />
-                      {type}
-                    </label>
-                  ))}
-                </div>
-                {errors['serviceDetails.electric_plumbing_types'] && <span className="error-text">{errors['serviceDetails.electric_plumbing_types']}</span>}
-              </div>
-            )}
-          </div>
-
-          {/* עבודות חוץ */}
-          <div style={{marginBottom: '20px'}}>
-            <label className="checkbox-item" style={{fontWeight: 'bold'}}>
-              <input
-                type="checkbox"
-                checked={serviceDetails.work_types?.includes('עבודות חוץ') || false}
-                onChange={(e) => {
-                  const current = serviceDetails.work_types || [];
-                  const newTypes = e.target.checked 
-                    ? [...current, 'עבודות חוץ']
-                    : current.filter(t => t !== 'עבודות חוץ');
-                  handleServiceDetailsChange('work_types', newTypes);
-                }}
-              />
-              עבודות חוץ
-            </label>
-            
-            {serviceDetails.work_types?.includes('עבודות חוץ') && (
-              <div style={{marginRight: '30px', marginTop: '10px'}}>
-                <div className="checkbox-group" data-field="exterior_work_types">
-                  {['ריצוף חוץ', 'בניית פרגולה', 'חיפויי אבן / חיפויי קירות חוץ', 'גידור', 'בניית שבילים בגינה'].map(type => (
-                    <label key={type} className="checkbox-item">
-                      <input
-                        type="checkbox"
-                        checked={serviceDetails.exterior_work_types?.includes(type) || false}
-                        onChange={(e) => {
-                          const current = serviceDetails.exterior_work_types || [];
-                          const newTypes = e.target.checked 
-                            ? [...current, type]
-                            : current.filter(t => t !== type);
-                          handleServiceDetailsChange('exterior_work_types', newTypes);
-                        }}
-                      />
-                      {type}
-                    </label>
-                  ))}
-                </div>
-                {errors['serviceDetails.exterior_work_types'] && <span className="error-text">{errors['serviceDetails.exterior_work_types']}</span>}
-              </div>
-            )}
-          </div>
-
-          {/* שיקום ותיקון חוץ */}
-          <div style={{marginBottom: '20px'}}>
-            <label className="checkbox-item" style={{fontWeight: 'bold'}}>
-              <input
-                type="checkbox"
-                checked={serviceDetails.work_types?.includes('שיקום ותיקון חוץ') || false}
-                onChange={(e) => {
-                  const current = serviceDetails.work_types || [];
-                  const newTypes = e.target.checked 
-                    ? [...current, 'שיקום ותיקון חוץ']
-                    : current.filter(t => t !== 'שיקום ותיקון חוץ');
-                  handleServiceDetailsChange('work_types', newTypes);
-                }}
-              />
-              שיקום ותיקון חוץ
-            </label>
-            
-            {serviceDetails.work_types?.includes('שיקום ותיקון חוץ') && (
-              <div style={{marginRight: '30px', marginTop: '10px'}}>
-                <div className="checkbox-group" data-field="facade_repair_types">
-                  {['תיקון טיח חוץ', 'שיקום קירות חיצוניים', 'איטום סדקים בקירות', 'טיפול בנפילת טיח'].map(type => (
-                    <label key={type} className="checkbox-item">
-                      <input
-                        type="checkbox"
-                        checked={serviceDetails.facade_repair_types?.includes(type) || false}
-                        onChange={(e) => {
-                          const current = serviceDetails.facade_repair_types || [];
-                          const newTypes = e.target.checked 
-                            ? [...current, type]
-                            : current.filter(t => t !== type);
-                          handleServiceDetailsChange('facade_repair_types', newTypes);
-                        }}
-                      />
-                      {type}
-                    </label>
-                  ))}
-                </div>
-                {errors['serviceDetails.facade_repair_types'] && <span className="error-text">{errors['serviceDetails.facade_repair_types']}</span>}
-              </div>
-            )}
-          </div>
-
-          {errors['serviceDetails.work_types'] && <span className="error-text">{errors['serviceDetails.work_types']}</span>}
-        </div>
-      </div>
-    </div>
-  );
-
- case 'aluminum':
-      return (
-        <div className="service-details-form">
-          <h3>פרטי שירות עבודות אלומיניום</h3>
-          
-          <div className="form-section">
-            <h4>שדות חובה</h4>
-            
-            <div className="input-group">
-              <label>גיל</label>
-              <input
-                type="number"
-                value={serviceDetails.age || ''}
-                onChange={(e) => handleServiceDetailsChange('age', e.target.value)}
-                className={`standard-input ${errors['serviceDetails.age'] ? 'error' : ''}`}
-                data-field="age"
-                min="18"
-              />
-              {errors['serviceDetails.age'] && <span className="error-text">{errors['serviceDetails.age']}</span>}
-            </div>
-
-            <div className="input-group">
-              <label>שנות ניסיון</label>
-              <input
-                type="number"
-                value={serviceDetails.experience || ''}
-                onChange={(e) => handleServiceDetailsChange('experience', e.target.value)}
-                className={`standard-input ${errors['serviceDetails.experience'] ? 'error' : ''}`}
-                data-field="experience"
-              />
-              {errors['serviceDetails.experience'] && <span className="error-text">{errors['serviceDetails.experience']}</span>}
-            </div>
-
-            <div className="input-group">
-              <label>ימי זמינות</label>
-              <div className="checkbox-group" data-field="availability_days">
-                {['ראשון', 'שני', 'שלישי', 'רביעי', 'חמישי', 'שישי', 'כל השבוע'].map(day => (
-                  <label key={day} className="checkbox-item">
-                    <input
-                      type="checkbox"
-                      checked={serviceDetails.availability_days?.includes(day) || false}
-                      onChange={() => handleExclusiveCheckbox('availability_days', day, 'כל השבוע', ['ראשון', 'שני', 'שלישי', 'רביעי', 'חמישי', 'שישי'])}
-                    />
-                    {day}
-                  </label>
-                ))}
-              </div>
-              {errors['serviceDetails.availability_days'] && <span className="error-text">{errors['serviceDetails.availability_days']}</span>}
-            </div>
-
-            <div className="input-group">
-              <label>שעות זמינות</label>
-              <div className="checkbox-group" data-field="availability_hours">
-                {['בוקר', 'אחר הצהריים', 'ערב', 'הכל'].map(hour => (
-                  <label key={hour} className="checkbox-item">
-                    <input
-                      type="checkbox"
-                      checked={serviceDetails.availability_hours?.includes(hour) || false}
-                      onChange={() => handleExclusiveCheckbox('availability_hours', hour, 'הכל', ['בוקר', 'אחר הצהריים', 'ערב'])}
-                    />
-                    {hour}
-                  </label>
-                ))}
-              </div>
-              {errors['serviceDetails.availability_hours'] && <span className="error-text">{errors['serviceDetails.availability_hours']}</span>}
-            </div>
-
-            <div className="input-group">
-              <label>סוגי עבודה</label>
-              
-              {/* חלונות ודלתות */}
-              <div style={{marginBottom: '20px'}}>
-                <label className="checkbox-item" style={{fontWeight: 'bold'}}>
-                  <input
-                    type="checkbox"
-                    checked={serviceDetails.work_types?.includes('חלונות ודלתות') || false}
-                    onChange={(e) => {
-                      const current = serviceDetails.work_types || [];
-                      const newTypes = e.target.checked 
-                        ? [...current, 'חלונות ודלתות']
-                        : current.filter(t => t !== 'חלונות ודלתות');
-                      handleServiceDetailsChange('work_types', newTypes);
-                    }}
-                  />
-                  חלונות ודלתות
-                </label>
-                
-                {serviceDetails.work_types?.includes('חלונות ודלתות') && (
-                  <div style={{marginRight: '30px', marginTop: '10px'}}>
-                    <div className="checkbox-group" data-field="windows_doors_types">
-                      {['התקנת חלונות אלומיניום', 'דלתות אלומיניום', 'דלתות הזזה (ויטרינות)', 'דלתות כניסה מאלומיניום', 'רשתות נגד יתושים', 'תריסים ידניים', 'תריסים חשמליים'].map(type => (
-                        <label key={type} className="checkbox-item">
-                          <input
-                            type="checkbox"
-                            checked={serviceDetails.windows_doors_types?.includes(type) || false}
-                            onChange={(e) => {
-                              const current = serviceDetails.windows_doors_types || [];
-                              const newTypes = e.target.checked 
-                                ? [...current, type]
-                                : current.filter(t => t !== type);
-                              handleServiceDetailsChange('windows_doors_types', newTypes);
-                            }}
-                          />
-                          {type}
-                        </label>
-                      ))}
-                    </div>
-                    {errors['serviceDetails.windows_doors_types'] && <span className="error-text">{errors['serviceDetails.windows_doors_types']}</span>}
-                  </div>
-                )}
-              </div>
-
-              {/* פרגולות ואלומיניום חוץ */}
-              <div style={{marginBottom: '20px'}}>
-                <label className="checkbox-item" style={{fontWeight: 'bold'}}>
-                  <input
-                    type="checkbox"
-                    checked={serviceDetails.work_types?.includes('פרגולות ואלומיניום חוץ') || false}
-                    onChange={(e) => {
-                      const current = serviceDetails.work_types || [];
-                      const newTypes = e.target.checked 
-                        ? [...current, 'פרגולות ואלומיניום חוץ']
-                        : current.filter(t => t !== 'פרגולות ואלומיניום חוץ');
-                      handleServiceDetailsChange('work_types', newTypes);
-                    }}
-                  />
-                  פרגולות ואלומיניום חוץ
-                </label>
-                
-                {serviceDetails.work_types?.includes('פרגולות ואלומיניום חוץ') && (
-                  <div style={{marginRight: '30px', marginTop: '10px'}}>
-                    <div className="checkbox-group" data-field="pergolas_outdoor_types">
-                      {['פרגולות אלומיניום', 'סגירת מרפסות', 'חיפויי אלומיניום חיצוניים', 'מעקות אלומיניום לגינה / מרפסות'].map(type => (
-                        <label key={type} className="checkbox-item">
-                          <input
-                            type="checkbox"
-                            checked={serviceDetails.pergolas_outdoor_types?.includes(type) || false}
-                            onChange={(e) => {
-                              const current = serviceDetails.pergolas_outdoor_types || [];
-                              const newTypes = e.target.checked 
-                                ? [...current, type]
-                                : current.filter(t => t !== type);
-                              handleServiceDetailsChange('pergolas_outdoor_types', newTypes);
-                            }}
-                          />
-                          {type}
-                        </label>
-                      ))}
-                    </div>
-                    {errors['serviceDetails.pergolas_outdoor_types'] && <span className="error-text">{errors['serviceDetails.pergolas_outdoor_types']}</span>}
-                  </div>
-                )}
-              </div>
-
-              {/* תיקונים ושירות */}
-              <div style={{marginBottom: '20px'}}>
-                <label className="checkbox-item" style={{fontWeight: 'bold'}}>
-                  <input
-                    type="checkbox"
-                    checked={serviceDetails.work_types?.includes('תיקונים ושירות') || false}
-                    onChange={(e) => {
-                      const current = serviceDetails.work_types || [];
-                      const newTypes = e.target.checked 
-                        ? [...current, 'תיקונים ושירות']
-                        : current.filter(t => t !== 'תיקונים ושירות');
-                      handleServiceDetailsChange('work_types', newTypes);
-                    }}
-                  />
-                  תיקונים ושירות
-                </label>
-                
-                {serviceDetails.work_types?.includes('תיקונים ושירות') && (
-                  <div style={{marginRight: '30px', marginTop: '10px'}}>
-                    <div className="checkbox-group" data-field="repairs_service_types">
-                      {['תיקון מנועי תריס חשמלי', 'תיקון מסילות', 'תיקון גלגלים בחלונות', 'החלפת ידיות / צירים', 'איטום וחידוש מסביב לחלונות', 'תיקון תריסים ידניים'].map(type => (
-                        <label key={type} className="checkbox-item">
-                          <input
-                            type="checkbox"
-                            checked={serviceDetails.repairs_service_types?.includes(type) || false}
-                            onChange={(e) => {
-                              const current = serviceDetails.repairs_service_types || [];
-                              const newTypes = e.target.checked 
-                                ? [...current, type]
-                                : current.filter(t => t !== type);
-                              handleServiceDetailsChange('repairs_service_types', newTypes);
-                            }}
-                          />
-                          {type}
-                        </label>
-                      ))}
-                    </div>
-                    {errors['serviceDetails.repairs_service_types'] && <span className="error-text">{errors['serviceDetails.repairs_service_types']}</span>}
-                  </div>
-                )}
-              </div>
-
-               {/* חיפויי אלומיניום */}
-      <div style={{marginBottom: '20px'}}>
-        <label className="checkbox-item" style={{fontWeight: 'bold'}}>
-          <input
-            type="checkbox"
-            checked={serviceDetails.work_types?.includes('חיפויי אלומיניום') || false}
-            onChange={(e) => {
-              const current = serviceDetails.work_types || [];
-              const newTypes = e.target.checked 
-                ? [...current, 'חיפויי אלומיניום']
-                : current.filter(t => t !== 'חיפויי אלומיניום');
-              handleServiceDetailsChange('work_types', newTypes);
-            }}
-          />
-          חיפויי אלומיניום
-        </label>
-        
-        {serviceDetails.work_types?.includes('חיפויי אלומיניום') && (
-          <div style={{marginRight: '30px', marginTop: '10px'}}>
-            <div className="checkbox-group" data-field="cladding_types">
-              {['חיפוי צנרת / כיסוי צינורות', 'חיפוי מונים (חשמל / מים / גז)', 'ארגזים דקורטיביים מאלומיניום', 'חיפוי קווי מזגן', 'הגנה למנוע מזגן חיצוני', 'חיפוי קירות חוץ מאלומיניום', 'חיפויים דקורטיביים', 'חיפוי וארגזי תריס'].map(type => (
-                <label key={type} className="checkbox-item">
-                  <input
-                    type="checkbox"
-                    checked={serviceDetails.cladding_types?.includes(type) || false}
-                    onChange={(e) => {
-                      const current = serviceDetails.cladding_types || [];
-                      const newTypes = e.target.checked 
-                        ? [...current, type]
-                        : current.filter(t => t !== type);
-                      handleServiceDetailsChange('cladding_types', newTypes);
-                    }}
-                  />
-                  {type}
-                </label>
-              ))}
-            </div>
-            {errors['serviceDetails.cladding_types'] && <span className="error-text">{errors['serviceDetails.cladding_types']}</span>}
-          </div>
-        )}
-      </div>
-
-              {errors['serviceDetails.work_types'] && <span className="error-text">{errors['serviceDetails.work_types']}</span>}
-            </div>
-          </div>
-        </div>
-      );
-case 'glass_works':
-  return (
-    <div className="service-details-form">
-      <h3>פרטי שירות עבודות זכוכית</h3>
-      
-      <div className="form-section">
-        <h4>שדות חובה</h4>
-        
-        <div className="input-group">
-          <label>גיל</label>
-          <input
-            type="number"
-            value={serviceDetails.age || ''}
-            onChange={(e) => handleServiceDetailsChange('age', e.target.value)}
-            className={`standard-input ${errors['serviceDetails.age'] ? 'error' : ''}`}
-            data-field="age"
-            min="18"
-          />
-          {errors['serviceDetails.age'] && <span className="error-text">{errors['serviceDetails.age']}</span>}
-        </div>
-
-        <div className="input-group">
-          <label>שנות ניסיון</label>
-          <input
-            type="number"
-            value={serviceDetails.experience || ''}
-            onChange={(e) => handleServiceDetailsChange('experience', e.target.value)}
-            className={`standard-input ${errors['serviceDetails.experience'] ? 'error' : ''}`}
-            data-field="experience"
-          />
-          {errors['serviceDetails.experience'] && <span className="error-text">{errors['serviceDetails.experience']}</span>}
-        </div>
-
-        <div className="input-group">
-          <label>ימי זמינות</label>
-          <div className="checkbox-group" data-field="availability_days">
-            {['ראשון', 'שני', 'שלישי', 'רביעי', 'חמישי', 'שישי', 'כל השבוע'].map(day => (
-              <label key={day} className="checkbox-item">
-                <input
-                  type="checkbox"
-                  checked={serviceDetails.availability_days?.includes(day) || false}
-                  onChange={() => handleExclusiveCheckbox('availability_days', day, 'כל השבוע', ['ראשון', 'שני', 'שלישי', 'רביעי', 'חמישי', 'שישי'])}
-                />
-                {day}
-              </label>
-            ))}
-          </div>
-          {errors['serviceDetails.availability_days'] && <span className="error-text">{errors['serviceDetails.availability_days']}</span>}
-        </div>
-
-        <div className="input-group">
-          <label>שעות זמינות</label>
-          <div className="checkbox-group" data-field="availability_hours">
-            {['בוקר', 'אחר הצהריים', 'ערב', 'הכל'].map(hour => (
-              <label key={hour} className="checkbox-item">
-                <input
-                  type="checkbox"
-                  checked={serviceDetails.availability_hours?.includes(hour) || false}
-                  onChange={() => handleExclusiveCheckbox('availability_hours', hour, 'הכל', ['בוקר', 'אחר הצהריים', 'ערב'])}
-                />
-                {hour}
-              </label>
-            ))}
-          </div>
-          {errors['serviceDetails.availability_hours'] && <span className="error-text">{errors['serviceDetails.availability_hours']}</span>}
-        </div>
-
-        <div className="input-group">
-          <label>סוגי עבודה</label>
-          
-          {/* זכוכיות לבית */}
-          <div style={{marginBottom: '20px'}}>
-            <label className="checkbox-item" style={{fontWeight: 'bold'}}>
-              <input
-                type="checkbox"
-                checked={serviceDetails.work_types?.includes('זכוכיות לבית') || false}
-                onChange={(e) => {
-                  const current = serviceDetails.work_types || [];
-                  const newTypes = e.target.checked 
-                    ? [...current, 'זכוכיות לבית']
-                    : current.filter(t => t !== 'זכוכיות לבית');
-                  handleServiceDetailsChange('work_types', newTypes);
-                }}
-              />
-              זכוכיות לבית
-            </label>
-            
-            {serviceDetails.work_types?.includes('זכוכיות לבית') && (
-              <div style={{marginRight: '30px', marginTop: '10px'}}>
-                <div className="checkbox-group" data-field="home_glass_types">
-                  {['זכוכית לחלונות', 'זכוכית לבידוד רעש / בידוד חום', 'זכוכית מחוסמת – זכוכית טריפלקס', 'זכוכית חלבית / מט'].map(type => (
-                    <label key={type} className="checkbox-item">
-                      <input
-                        type="checkbox"
-                        checked={serviceDetails.home_glass_types?.includes(type) || false}
-                        onChange={(e) => {
-                          const current = serviceDetails.home_glass_types || [];
-                          const newTypes = e.target.checked 
-                            ? [...current, type]
-                            : current.filter(t => t !== type);
-                          handleServiceDetailsChange('home_glass_types', newTypes);
-                        }}
-                      />
-                      {type}
-                    </label>
-                  ))}
-                </div>
-                {errors['serviceDetails.home_glass_types'] && <span className="error-text">{errors['serviceDetails.home_glass_types']}</span>}
-              </div>
-            )}
-          </div>
-
-          {/* מקלחונים */}
-          <div style={{marginBottom: '20px'}}>
-            <label className="checkbox-item" style={{fontWeight: 'bold'}}>
-              <input
-                type="checkbox"
-                checked={serviceDetails.work_types?.includes('מקלחונים') || false}
-                onChange={(e) => {
-                  const current = serviceDetails.work_types || [];
-                  const newTypes = e.target.checked 
-                    ? [...current, 'מקלחונים']
-                    : current.filter(t => t !== 'מקלחונים');
-                  handleServiceDetailsChange('work_types', newTypes);
-                }}
-              />
-              מקלחונים
-            </label>
-            
-            {serviceDetails.work_types?.includes('מקלחונים') && (
-              <div style={{marginRight: '30px', marginTop: '10px'}}>
-                <div className="checkbox-group" data-field="shower_types">
-                  {['מקלחון חזית', 'מקלחון פינתי', 'מקלחון פתיחה / הזזה', 'התקנה / החלפה של זכוכית למקלחון'].map(type => (
-                    <label key={type} className="checkbox-item">
-                      <input
-                        type="checkbox"
-                        checked={serviceDetails.shower_types?.includes(type) || false}
-                        onChange={(e) => {
-                          const current = serviceDetails.shower_types || [];
-                          const newTypes = e.target.checked 
-                            ? [...current, type]
-                            : current.filter(t => t !== type);
-                          handleServiceDetailsChange('shower_types', newTypes);
-                        }}
-                      />
-                      {type}
-                    </label>
-                  ))}
-                </div>
-                {errors['serviceDetails.shower_types'] && <span className="error-text">{errors['serviceDetails.shower_types']}</span>}
-              </div>
-            )}
-          </div>
-
-          {/* מחיצות וקירות זכוכית */}
-          <div style={{marginBottom: '20px'}}>
-            <label className="checkbox-item" style={{fontWeight: 'bold'}}>
-              <input
-                type="checkbox"
-                checked={serviceDetails.work_types?.includes('מחיצות וקירות זכוכית') || false}
-                onChange={(e) => {
-                  const current = serviceDetails.work_types || [];
-                  const newTypes = e.target.checked 
-                    ? [...current, 'מחיצות וקירות זכוכית']
-                    : current.filter(t => t !== 'מחיצות וקירות זכוכית');
-                  handleServiceDetailsChange('work_types', newTypes);
-                }}
-              />
-              מחיצות וקירות זכוכית
-            </label>
-            
-            {serviceDetails.work_types?.includes('מחיצות וקירות זכוכית') && (
-              <div style={{marginRight: '30px', marginTop: '10px'}}>
-                <div className="checkbox-group" data-field="partition_types">
-                  {['מחיצות זכוכית לבית', 'קירות זכוכית ("Glass Wall")', 'חדרי ישיבות מזכוכית', 'דלתות זכוכית'].map(type => (
-                    <label key={type} className="checkbox-item">
-                      <input
-                        type="checkbox"
-                        checked={serviceDetails.partition_types?.includes(type) || false}
-                        onChange={(e) => {
-                          const current = serviceDetails.partition_types || [];
-                          const newTypes = e.target.checked 
-                            ? [...current, type]
-                            : current.filter(t => t !== type);
-                          handleServiceDetailsChange('partition_types', newTypes);
-                        }}
-                      />
-                      {type}
-                    </label>
-                  ))}
-                </div>
-                {errors['serviceDetails.partition_types'] && <span className="error-text">{errors['serviceDetails.partition_types']}</span>}
-              </div>
-            )}
-          </div>
-
-          {/* מעקות זכוכית */}
-          <div style={{marginBottom: '20px'}}>
-            <label className="checkbox-item" style={{fontWeight: 'bold'}}>
-              <input
-                type="checkbox"
-                checked={serviceDetails.work_types?.includes('מעקות זכוכית') || false}
-                onChange={(e) => {
-                  const current = serviceDetails.work_types || [];
-                  const newTypes = e.target.checked 
-                    ? [...current, 'מעקות זכוכית']
-                    : current.filter(t => t !== 'מעקות זכוכית');
-                  handleServiceDetailsChange('work_types', newTypes);
-                }}
-              />
-              מעקות זכוכית
-            </label>
-            
-            {serviceDetails.work_types?.includes('מעקות זכוכית') && (
-              <div style={{marginRight: '30px', marginTop: '10px'}}>
-                <div className="checkbox-group" data-field="railing_types">
-                  {['מעקה זכוכית למרפסת', 'מעקה זכוכית למדרגות', 'חיבורי נירוסטה / פרופילים'].map(type => (
-                    <label key={type} className="checkbox-item">
-                      <input
-                        type="checkbox"
-                        checked={serviceDetails.railing_types?.includes(type) || false}
-                        onChange={(e) => {
-                          const current = serviceDetails.railing_types || [];
-                          const newTypes = e.target.checked 
-                            ? [...current, type]
-                            : current.filter(t => t !== type);
-                          handleServiceDetailsChange('railing_types', newTypes);
-                        }}
-                      />
-                      {type}
-                    </label>
-                  ))}
-                </div>
-                {errors['serviceDetails.railing_types'] && <span className="error-text">{errors['serviceDetails.railing_types']}</span>}
-              </div>
-            )}
-          </div>
-
-          {/* ריהוט וזכוכית מעוצבת */}
-          <div style={{marginBottom: '20px'}}>
-            <label className="checkbox-item" style={{fontWeight: 'bold'}}>
-              <input
-                type="checkbox"
-                checked={serviceDetails.work_types?.includes('ריהוט וזכוכית מעוצבת') || false}
-                onChange={(e) => {
-                  const current = serviceDetails.work_types || [];
-                  const newTypes = e.target.checked 
-                    ? [...current, 'ריהוט וזכוכית מעוצבת']
-                    : current.filter(t => t !== 'ריהוט וזכוכית מעוצבת');
-                  handleServiceDetailsChange('work_types', newTypes);
-                }}
-              />
-              ריהוט וזכוכית מעוצבת
-            </label>
-            
-            {serviceDetails.work_types?.includes('ריהוט וזכוכית מעוצבת') && (
-              <div style={{marginRight: '30px', marginTop: '10px'}}>
-                <div className="checkbox-group" data-field="furniture_glass_types">
-                  {['שולחנות זכוכית לפי מידה', 'מדפי זכוכית', 'פלטות זכוכית לשולחן', 'מראות מיוחדות בעיצוב אישי'].map(type => (
-                    <label key={type} className="checkbox-item">
-                      <input
-                        type="checkbox"
-                        checked={serviceDetails.furniture_glass_types?.includes(type) || false}
-                        onChange={(e) => {
-                          const current = serviceDetails.furniture_glass_types || [];
-                          const newTypes = e.target.checked 
-                            ? [...current, type]
-                            : current.filter(t => t !== type);
-                          handleServiceDetailsChange('furniture_glass_types', newTypes);
-                        }}
-                      />
-                      {type}
-                    </label>
-                  ))}
-                </div>
-                {errors['serviceDetails.furniture_glass_types'] && <span className="error-text">{errors['serviceDetails.furniture_glass_types']}</span>}
-              </div>
-            )}
-          </div>
-
-          {/* תיקונים ושירות */}
-          <div style={{marginBottom: '20px'}}>
-            <label className="checkbox-item" style={{fontWeight: 'bold'}}>
-              <input
-                type="checkbox"
-                checked={serviceDetails.work_types?.includes('תיקונים ושירות') || false}
-                onChange={(e) => {
-                  const current = serviceDetails.work_types || [];
-                  const newTypes = e.target.checked 
-                    ? [...current, 'תיקונים ושירות']
-                    : current.filter(t => t !== 'תיקונים ושירות');
-                  handleServiceDetailsChange('work_types', newTypes);
-                }}
-              />
-              תיקונים ושירות
-            </label>
-            
-            {serviceDetails.work_types?.includes('תיקונים ושירות') && (
-              <div style={{marginRight: '30px', marginTop: '10px'}}>
-                <div className="checkbox-group" data-field="repair_service_types">
-                  {['החלפת זכוכית שבורה', 'החלפת חלון כפול', 'תיקון דלתות זכוכית', 'החלפת מחברים / צירים', 'אטימה סביב חלונות זכוכית'].map(type => (
-                    <label key={type} className="checkbox-item">
-                      <input
-                        type="checkbox"
-                        checked={serviceDetails.repair_service_types?.includes(type) || false}
-                        onChange={(e) => {
-                          const current = serviceDetails.repair_service_types || [];
-                          const newTypes = e.target.checked 
-                            ? [...current, type]
-                            : current.filter(t => t !== type);
-                          handleServiceDetailsChange('repair_service_types', newTypes);
-                        }}
-                      />
-                      {type}
-                    </label>
-                  ))}
-                </div>
-                {errors['serviceDetails.repair_service_types'] && <span className="error-text">{errors['serviceDetails.repair_service_types']}</span>}
-              </div>
-            )}
-          </div>
-
-          {errors['serviceDetails.work_types'] && <span className="error-text">{errors['serviceDetails.work_types']}</span>}
-        </div>
-      </div>
-    </div>
-  );
-case 'locksmith':
-  return (
-    <div className="service-details-form">
-      <h3>פרטי שירות מנעולן</h3>
-      
-      <div className="form-section">
-        <h4>שדות חובה</h4>
-        
-        <div className="input-group">
-          <label>גיל</label>
-          <input
-            type="number"
-            value={serviceDetails.age || ''}
-            onChange={(e) => handleServiceDetailsChange('age', e.target.value)}
-            className={`standard-input ${errors['serviceDetails.age'] ? 'error' : ''}`}
-            data-field="age"
-            min="18"
-          />
-          {errors['serviceDetails.age'] && <span className="error-text">{errors['serviceDetails.age']}</span>}
-        </div>
-
-        <div className="input-group">
-          <label>שנות ניסיון</label>
-          <input
-            type="number"
-            value={serviceDetails.experience || ''}
-            onChange={(e) => handleServiceDetailsChange('experience', e.target.value)}
-            className={`standard-input ${errors['serviceDetails.experience'] ? 'error' : ''}`}
-            data-field="experience"
-          />
-          {errors['serviceDetails.experience'] && <span className="error-text">{errors['serviceDetails.experience']}</span>}
-        </div>
-
-        <div className="input-group">
-          <label>ימי זמינות</label>
-          <div className="checkbox-group" data-field="availability_days">
-            {['ראשון', 'שני', 'שלישי', 'רביעי', 'חמישי', 'שישי', 'כל השבוע'].map(day => (
-              <label key={day} className="checkbox-item">
-                <input
-                  type="checkbox"
-                  checked={serviceDetails.availability_days?.includes(day) || false}
-                  onChange={() => handleExclusiveCheckbox('availability_days', day, 'כל השבוע', ['ראשון', 'שני', 'שלישי', 'רביעי', 'חמישי', 'שישי'])}
-                />
-                {day}
-              </label>
-            ))}
-          </div>
-          {errors['serviceDetails.availability_days'] && <span className="error-text">{errors['serviceDetails.availability_days']}</span>}
-        </div>
-
-        <div className="input-group">
-          <label>שעות זמינות</label>
-          <div className="checkbox-group" data-field="availability_hours">
-            {['בוקר', 'אחר הצהריים', 'ערב', 'הכל'].map(hour => (
-              <label key={hour} className="checkbox-item">
-                <input
-                  type="checkbox"
-                  checked={serviceDetails.availability_hours?.includes(hour) || false}
-                  onChange={() => handleExclusiveCheckbox('availability_hours', hour, 'הכל', ['בוקר', 'אחר הצהריים', 'ערב'])}
-                />
-                {hour}
-              </label>
-            ))}
-          </div>
-          {errors['serviceDetails.availability_hours'] && <span className="error-text">{errors['serviceDetails.availability_hours']}</span>}
-        </div>
-
-        <div className="input-group">
-          <label>סוגי עבודה</label>
-          
-          {/* פריצות חירום */}
-          <div style={{marginBottom: '20px'}}>
-            <label className="checkbox-item" style={{fontWeight: 'bold'}}>
-              <input
-                type="checkbox"
-                checked={serviceDetails.work_types?.includes('🔐 פריצות חירום') || false}
-                onChange={(e) => {
-                  const current = serviceDetails.work_types || [];
-                  const newTypes = e.target.checked 
-                    ? [...current, '🔐 פריצות חירום']
-                    : current.filter(t => t !== '🔐 פריצות חירום');
-                  handleServiceDetailsChange('work_types', newTypes);
-                }}
-              />
-              🔐 פריצות חירום
-            </label>
-            
-            {serviceDetails.work_types?.includes('🔐 פריצות חירום') && (
-              <div style={{marginRight: '30px', marginTop: '10px'}}>
-                <div className="checkbox-group" data-field="emergency_opening_types">
-                  {['פריצת דלתות בית', 'פריצת דלת פנים', 'פריצת דלת נעולה ללא נזק', 'פריצת דלת עם מפתח שבור', 'פריצת רכב', 'פריצת כספת', 'פתיחת מחסן נעול'].map(type => (
-                    <label key={type} className="checkbox-item">
-                      <input
-                        type="checkbox"
-                        checked={serviceDetails.emergency_opening_types?.includes(type) || false}
-                        onChange={(e) => {
-                          const current = serviceDetails.emergency_opening_types || [];
-                          const newTypes = e.target.checked 
-                            ? [...current, type]
-                            : current.filter(t => t !== type);
-                          handleServiceDetailsChange('emergency_opening_types', newTypes);
-                        }}
-                      />
-                      {type}
-                    </label>
-                  ))}
-                </div>
-                {errors['serviceDetails.emergency_opening_types'] && <span className="error-text">{errors['serviceDetails.emergency_opening_types']}</span>}
-              </div>
-            )}
-          </div>
-
-          {/* החלפת מנעולים */}
-          <div style={{marginBottom: '20px'}}>
-            <label className="checkbox-item" style={{fontWeight: 'bold'}}>
-              <input
-                type="checkbox"
-                checked={serviceDetails.work_types?.includes('🔑 החלפת מנעולים') || false}
-                onChange={(e) => {
-                  const current = serviceDetails.work_types || [];
-                  const newTypes = e.target.checked 
-                    ? [...current, '🔑 החלפת מנעולים']
-                    : current.filter(t => t !== '🔑 החלפת מנעולים');
-                  handleServiceDetailsChange('work_types', newTypes);
-                }}
-              />
-              🔑 החלפת מנעולים
-            </label>
-            
-            {serviceDetails.work_types?.includes('🔑 החלפת מנעולים') && (
-              <div style={{marginRight: '30px', marginTop: '10px'}}>
-                <div className="checkbox-group" data-field="lock_replacement_types">
-                  {['החלפת צילינדר', 'התקנת צילינדר נגד פריצה', 'התקנת מנעולי רב־בריח', 'התקנת מנעולי עליון', 'התקנת מנעול חכם / דיגיטלי'].map(type => (
-                    <label key={type} className="checkbox-item">
-                      <input
-                        type="checkbox"
-                        checked={serviceDetails.lock_replacement_types?.includes(type) || false}
-                        onChange={(e) => {
-                          const current = serviceDetails.lock_replacement_types || [];
-                          const newTypes = e.target.checked 
-                            ? [...current, type]
-                            : current.filter(t => t !== type);
-                          handleServiceDetailsChange('lock_replacement_types', newTypes);
-                        }}
-                      />
-                      {type}
-                    </label>
-                  ))}
-                </div>
-                {errors['serviceDetails.lock_replacement_types'] && <span className="error-text">{errors['serviceDetails.lock_replacement_types']}</span>}
-              </div>
-            )}
-          </div>
-
-          {/* תיקון דלתות ובטיחות */}
-          <div style={{marginBottom: '20px'}}>
-            <label className="checkbox-item" style={{fontWeight: 'bold'}}>
-              <input
-                type="checkbox"
-                checked={serviceDetails.work_types?.includes('🚪 תיקון דלתות ובטיחות') || false}
-                onChange={(e) => {
-                  const current = serviceDetails.work_types || [];
-                  const newTypes = e.target.checked 
-                    ? [...current, '🚪 תיקון דלתות ובטיחות']
-                    : current.filter(t => t !== '🚪 תיקון דלתות ובטיחות');
-                  handleServiceDetailsChange('work_types', newTypes);
-                }}
-              />
-              🚪 תיקון דלתות ובטיחות
-            </label>
-            
-            {serviceDetails.work_types?.includes('🚪 תיקון דלתות ובטיחות') && (
-              <div style={{marginRight: '30px', marginTop: '10px'}}>
-                <div className="checkbox-group" data-field="door_repair_types">
-                  {['תיקון דלת תקועה / לא נסגרת', 'יישור דלת / כיוון דלת', 'תיקון צירים / מנגנון', 'שדרוג דלת רגילה לדלת ביטחון', 'התקנת מנגנוני בטיחות'].map(type => (
-                    <label key={type} className="checkbox-item">
-                      <input
-                        type="checkbox"
-                        checked={serviceDetails.door_repair_types?.includes(type) || false}
-                        onChange={(e) => {
-                          const current = serviceDetails.door_repair_types || [];
-                          const newTypes = e.target.checked 
-                            ? [...current, type]
-                            : current.filter(t => t !== type);
-                          handleServiceDetailsChange('door_repair_types', newTypes);
-                        }}
-                      />
-                      {type}
-                    </label>
-                  ))}
-                </div>
-                {errors['serviceDetails.door_repair_types'] && <span className="error-text">{errors['serviceDetails.door_repair_types']}</span>}
-              </div>
-            )}
-          </div>
-
-          {/* מערכות מתקדמות */}
-          <div style={{marginBottom: '20px'}}>
-            <label className="checkbox-item" style={{fontWeight: 'bold'}}>
-              <input
-                type="checkbox"
-                checked={serviceDetails.work_types?.includes('📡 מערכות מתקדמות') || false}
-                onChange={(e) => {
-                  const current = serviceDetails.work_types || [];
-                  const newTypes = e.target.checked 
-                    ? [...current, '📡 מערכות מתקדמות']
-                    : current.filter(t => t !== '📡 מערכות מתקדמות');
-                  handleServiceDetailsChange('work_types', newTypes);
-                }}
-              />
-              📡 מערכות מתקדמות
-            </label>
-            
-            {serviceDetails.work_types?.includes('📡 מערכות מתקדמות') && (
-              <div style={{marginRight: '30px', marginTop: '10px'}}>
-                <div className="checkbox-group" data-field="advanced_systems_types">
-                  {['מנעולים חכמים', 'קודן / מערכת כניסה ללא מפתח', 'מערכות שליטה מרחוק (באפליקציה)'].map(type => (
-                    <label key={type} className="checkbox-item">
-                      <input
-                        type="checkbox"
-                        checked={serviceDetails.advanced_systems_types?.includes(type) || false}
-                        onChange={(e) => {
-                          const current = serviceDetails.advanced_systems_types || [];
-                          const newTypes = e.target.checked 
-                            ? [...current, type]
-                            : current.filter(t => t !== type);
-                          handleServiceDetailsChange('advanced_systems_types', newTypes);
-                        }}
-                      />
-                      {type}
-                    </label>
-                  ))}
-                </div>
-                {errors['serviceDetails.advanced_systems_types'] && <span className="error-text">{errors['serviceDetails.advanced_systems_types']}</span>}
-              </div>
-            )}
-          </div>
-
-          {errors['serviceDetails.work_types'] && <span className="error-text">{errors['serviceDetails.work_types']}</span>}
-        </div>
-      </div>
-    </div>
-  );
-    default:
-      return null;
-  }
-};
-
   const renderWorkingAreasSection = () => {
     if (mode !== 'register' || step !== 2) return null;
 
@@ -6392,7 +1667,13 @@ case 'locksmith':
   {imageError && <span className="error-text">{imageError}</span>}
 </div>
 
-              {renderServiceDetailsForm()}
+             <ServiceDetailsForm 
+  serviceType={formData.serviceType}
+  serviceDetails={formData.serviceDetails}
+  errors={errors}
+  handleServiceDetailsChange={handleServiceDetailsChange}
+  handleExclusiveCheckbox={handleExclusiveCheckbox}
+/>
 
               <div className="step-navigation">
                 <button 
